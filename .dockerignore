# ── ความลับ ต้องไม่ถูก bake เข้า image ──────────────────────────────
# image ถูกส่งต่อ/push ขึ้น registry ได้ แต่ของพวกนี้ต้องอยู่เฉพาะเครื่องปลายทาง
.env
.env.local
config/users.json
config/users.json.tmp
username.txt

# ── ข้อมูลที่เกิดตอนรัน — ต้องมาจาก volume ไม่ใช่จาก image ──────────
# ถ้าติดไปด้วย ทะเบียนใบขอซื้อของ image จะไปทับของจริงตอน mount ผิดพลาด
data/

# ── ของที่ไม่เกี่ยวกับการรันเซิร์ฟเวอร์ ─────────────────────────────
.git
.gitignore
.claude/
node_modules/
*.log

# เอกสารต้นฉบับและไฟล์ตัวอย่างที่ใช้ตอนพัฒนา
Purchase Request/
*.docx
*.xlsx
*.url
*.bat
logo/
README.md
READ.md

# บันทึกระหว่างพัฒนา — ไม่เกี่ยวกับการรัน
ปัญหาข้อมูลในชีต - ต้องแก้ที่ต้นทาง.txt
สร้าง Dashboard แสดงข้อมูลรายงาน Ex.txt

# "แบบฟอร์มรายงาน Kambis.txt" **ต้องติดไปกับ image**
# เป็นแหล่งความจริงของลิงก์ชีต ถ้าไม่มี จะรัน sync-sources.js ในคอนเทนเนอร์ไม่ได้
# ตอนที่คนเพิ่มลิงก์ชีตใหม่ (ดู deploy/README.md) — และไม่ได้ทำให้ปลอดภัยขึ้น
# เพราะ sheetId ทุกอันอยู่ใน config/sources.json ที่ติดไปกับ image อยู่แล้ว
