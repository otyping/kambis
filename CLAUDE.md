# CLAUDE.md — Kambis Executive Report Dashboard

Dashboard สรุปรายงานผู้บริหารของ Kambis อ่านข้อมูลสดจาก Google Sheets
เขียนด้วย HTML / CSS / Vanilla JavaScript / Three.js และ Node.js server แบบไม่มี dependency

---

## 1. กฎการอ่านค่ารายงาน (สำคัญที่สุด)

> **ลิงก์รายงานทุกอันต้องอ่านจากไฟล์ `แบบฟอร์มรายงาน Kambis.txt` เท่านั้น**

ไฟล์ `แบบฟอร์มรายงาน Kambis.txt` คือ **แหล่งความจริงเพียงแหล่งเดียว (single source of truth)**
ของลิงก์ Google Sheets ทั้งหมด ในนั้นมีลิงก์รายงานหลายอัน แต่ละอันเป็นคนละรายงานกัน

ขั้นตอนการอ่านค่า:

```
แบบฟอร์มรายงาน Kambis.txt          ← ไฟล์ต้นทาง (คนแก้ด้วยมือ)
        │  node scripts/sync-sources.js
        ▼
config/sources.json                 ← ไฟล์ generated (โค้ดอ่านจากตรงนี้)
        │  server/lib/fetcher.js
        ▼
https://docs.google.com/spreadsheets/d/{sheetId}/gviz/tq?tqx=out:csv&gid={gid}
        │  server/lib/csv.js → server/lib/parsers/*.js
        ▼
server/lib/analysis.js              ← ตรวจสอบความถูกต้องทุกครั้ง (บังคับ)
        ▼
GET /api/reports                    ← front-end ใช้
```

**ข้อห้าม**

- ❌ ห้ามเขียน URL ของ Google Sheets ลงในไฟล์ source ใด ๆ นอกจาก `config/sources.json`
  (ตรวจได้ด้วย `grep -rn "docs.google.com" server public scripts` — ต้องไม่เจอ)
- ❌ ห้ามแก้ไขไฟล์ `แบบฟอร์มรายงาน Kambis.txt` ด้วยโค้ด ไฟล์นี้คนเป็นคนแก้
- ❌ ห้าม commit ค่าข้อมูลจากชีตเป็น fixture ถาวร (cache ใน `data/` ถูก gitignore ไว้แล้ว)

**เมื่อลิงก์ในไฟล์ .txt เปลี่ยน / เพิ่ม / ลบ** ให้รัน:

```bash
node scripts/sync-sources.js
```

สคริปต์จะอ่านคู่ `ชื่อรายงาน:` + `URL` จากไฟล์ .txt แล้วไปสำรวจ tab ทั้งหมดของแต่ละ spreadsheet
(ผ่าน `…/htmlview`) และเขียน `config/sources.json` ใหม่
เซิร์ฟเวอร์จะเตือนตอนเริ่มทำงานถ้าไฟล์ .txt ถูกแก้หลัง `sources.json` ถูก generate
(เทียบกับฟิลด์ `sourceMtime` ในไฟล์ ไม่ใช่ mtime ของตัวไฟล์ เพราะเซิร์ฟเวอร์เขียนทับ `tabs` เองได้)

### แท็บใหม่ถูกค้นหาอัตโนมัติทุกครั้งที่รีเฟรช

`server/lib/loader.js` เรียก `discoverTabs()` ก่อนโหลดทุกรายงาน จึงรับแท็บที่เพิ่งเพิ่มในชีต
(เช่นเดือนใหม่ ครอปใหม่) ได้เองโดยไม่ต้องรัน `sync-sources.js`

| เหตุการณ์ในชีต | กด Refresh แล้วเห็นไหม |
|---|---|
| แก้ตัวเลข / เพิ่มแถวในแท็บเดิม | เห็นทันที |
| **เพิ่มแท็บใหม่** | **เห็นทันที** — และขึ้นแถบแจ้งบนหน้าเว็บว่าเจอแท็บอะไร |
| ลบหรือเปลี่ยนชื่อแท็บ | เห็นทันที พร้อมแจ้งเตือน |
| เพิ่มลิงก์ชีตใหม่ในไฟล์ .txt | **ต้องรัน `node scripts/sync-sources.js`** |

รายละเอียดการทำงาน:

- ค้นจาก `…/htmlview` ซึ่งเป็นหน้าเล็ก (~50 KB เพราะเนื้อตารางโหลดทีหลัง) ใช้เวลา ~0.6 วิ/ชีต
- ถ้าค้นไม่สำเร็จ (เน็ตล่ม / ชีตถูกปิดแชร์) จะ **fallback ไปใช้รายชื่อใน `config/sources.json`**
  แล้วรายงานสถานะเป็น `discovery: "config"` — ไม่ทำให้ทั้งรายงานล่ม
- ถ้าหน้าโหลดได้แต่ไม่เจอแท็บเลย ถือว่า **ล้มเหลว** ไม่ใช่ "ชีตนี้ไม่มีแท็บ"
  (ไม่งั้นข้อมูลทั้งรายงานจะหายเงียบ ๆ)
- เมื่อรายชื่อเปลี่ยน จะเขียนกลับลง `config/sources.json` แบบ atomic (เขียน `.tmp` แล้ว rename)
  เพื่อให้รายชื่อล่าสุดยังใช้ได้ตอนออฟไลน์

> `config/sources.json` ยังเป็นไฟล์ generated เหมือนเดิม — ส่วนที่เซิร์ฟเวอร์แตะได้มีแค่ `tabs[]`
> ส่วน `sheetId` / `parser` / ลิงก์ ยังมาจากไฟล์ .txt ผ่าน `sync-sources.js` เท่านั้น

---

## 2. กฎการวิเคราะห์ข้อมูล (บังคับทุกครั้งที่อัปเดต)

> **ทุกครั้งที่ Dashboard มีการอัปเดตข้อมูล ต้องรัน Data Analysis ตรวจและวิเคราะห์ความถูกต้องเสมอ**

`server/lib/analysis.js` จะทำงานอัตโนมัติทุกครั้งที่มีการดึงข้อมูล — ทั้งตอนเปิดหน้าเว็บ
และตอนกดปุ่ม Refresh — ไม่มี code path ไหนที่ข้ามได้

ผลลัพธ์ถูกแนบไปกับ payload ทุกครั้ง และแสดงบน UI 3 จุด:
badge คุณภาพข้อมูลบน header, chip บนการ์ดแต่ละใบ, และการ์ด "คุณภาพข้อมูล" ที่ลงรายละเอียดทุก finding

### หลักการวิเคราะห์

1. **คำนวณใหม่เสมอ ไม่เชื่อคอลัมน์ Total / % ในชีต** — sum เองจากคอลัมน์ขนาด แล้วเทียบกับที่ชีตบอก
2. **ห้ามซ่อมข้อมูลเงียบ ๆ** — ค่าดิบเก็บไว้ใน `raw.*`, ค่าที่คำนวณใหม่แสดงบน UI, ความต่างออกเป็น finding
3. **`-` และเซลล์ว่าง = `null` ไม่ใช่ `0`** — ในชีตนี้ `-` แปลว่าไม่มีข้อมูล การนับเป็น 0 ทำให้ค่าเฉลี่ยผิด

### กลุ่มการตรวจสอบ

| กลุ่ม | ตรวจอะไร |
|---|---|
| Structural | เข้าถึง tab ได้, หา header row เจอ, มีแถวข้อมูล, คอลัมน์ครบ, **แต่ละ tab อ่านข้อมูลได้จริงไหม** |
| Arithmetic | Σ ขนาด = total ที่ชีตบอก (tol 0.5 g), % ที่คำนวณ = % ที่ชีตบอก, แถว Total = Σ แถวข้อมูล |
| Range | 0 ≤ % ≤ 100, น้ำหนัก ≥ 0, outlier > 3× median |
| Units | label บอกหน่วยหนึ่งแต่ค่าเป็นอีกหน่วย |
| Dates | parse ได้, เรียงถูก, ไม่เกิน 12 เดือนในอนาคต, ชื่อ tab ตรงกับวันที่ในชีต |
| Completeness | มีน้ำหนักแต่ total ว่าง, ไม่มี crop/strain, แถวซ้ำ |
| Cross-source | ขนออกจากฟาร์ม ≈ รับที่กรุงเทพ, Σ รายวัน ≈ ยอดต่อครอป, ยอดขาย ≤ ของที่รับเข้า |
| **Presentation** | **ลำดับที่นำเสนอถูกไหม — ไตรมาส/เดือน/วันเรียงตามเวลาจริง** |

### การตรวจรายแท็บ — กันข้อมูลหายเงียบ ๆ

ออกแบบมาเพื่อกรณีที่ **ไม่มี dev เฝ้า**: คนเพิ่มแท็บใหม่ในชีตเอง แล้วระบบอ่านไม่ออก
ถ้าไม่มีการตรวจนี้ จำนวนแท็บจะเพิ่มขึ้นแต่ยอดรวมไม่ขยับ โดยไม่มีอะไรเตือนเลย

| finding | เมื่อไหร่ | ระดับ |
|---|---|---|
| `structural.tabEmpty` | โหลดได้แต่อ่านไม่ได้สักแถว | `warning` — เว้นแต่ชื่อเข้าแพตเทิร์นแท็บเปล่า (`ชีต5`, `สำเนาของ`) → `info` |
| `structural.tabIgnored` | ถูกข้ามด้วยแพตเทิร์นชื่อ | `info` ถ้าเป็น `template`/`summary` · `warning` ถ้าเป็นเหตุผลอื่น |

**กฎสำคัญ:** ต้องกรองแท็บที่ parser ตั้งใจไม่อ่านออกก่อนเสมอ ดูจากมาร์กที่ parser ใส่ไว้

- `detailOnly: true` — `perCrop` มีแท็บรายครอป 37 อันแบบนี้ (ข้อมูลจริงอยู่ใน `SUMMARY SHEET`)
  **ถ้าลืมกรอง จะได้ false alarm 37 อันทันที** มี test กันไว้แล้ว
- `skipped: 'template' | 'summary'` — แท็บที่ระบบรู้จักและตั้งใจข้าม

`checkTabProducedData()` ใน `analysis.js` เป็นคนตรวจ ใช้ข้อมูลที่ parser ส่งมาอยู่แล้ว
(`rowCount`, `skipped`, `detailOnly`) ผ่าน `loader.js` — ไม่ต้องแก้ parser

### การตรวจชั้นที่สอง: `verifyPresentation(analysis, kpi, sources)`

`analyze()` ตรวจข้อมูลที่ **อ่านเข้ามา** แต่ตัวเลขที่ผู้บริหารเห็นบนจอผ่านการจัดกลุ่ม
และเรียงลำดับของ `aggregate.js` อีกชั้นหนึ่ง ความผิดพลาดที่เกิดในขั้นนำเสนอไม่มีอะไรจับได้เลย

เคสจริงที่เคยหลุด: `byQuarter` เรียงด้วย `localeCompare` ทำให้ได้
`Q1'2026 → Q2'2025 → Q2'2026` เพราะเทียบ "Q1" กับ "Q2" ก่อนถึงปี
กราฟดูเหมือนผลผลิตตกแล้วเด้ง ทั้งที่ความจริงเรียงผิด

นอกจากลำดับเวลาแล้ว ชั้นนี้ยัง **บวกตัวเลขที่ขึ้นจอใหม่ด้วยลูปธรรมดา** แล้วเทียบกับ `kpi.*`
(น้ำหนักดอกทุกรายงาน · รายได้/ต้นทุน/ค่าเสื่อม · จำนวนของที่ต้องสั่งซื้อ)
**ห้ามเรียกฟังก์ชันชุดเดียวกับที่ `buildKpi` ใช้** ไม่งั้นจะได้คำตอบเดิมเสมอ ซึ่งพิสูจน์อะไรไม่ได้
ไม่ตรงเมื่อไหร่ออกเป็น `presentation.totalMismatch` (critical) ซึ่งแปลว่า **Dashboard ผิดเอง ไม่ใช่ชีตผิด**

`verifyPresentation(analysis, kpi, sources)` ถูกเรียกใน `loader.js` **หลัง** `buildKpi()`
แล้วผนวก finding เข้ากับผลวิเคราะห์เดิมพร้อมคิดคะแนนใหม่ ตรวจว่า `byQuarter`,
`byMonth` และ `series` ของทุกรายงานเรียงตามเวลาจริง (finding id `order.notChronological`)

**กฎ:** ป้ายช่วงเวลาห้ามเรียงด้วย `localeCompare` เด็ดขาด ให้ใช้ `comparePeriod()`
จาก `shared/agg-core.js` เสมอ (ทั้ง server และเบราว์เซอร์ import ไฟล์เดียวกัน)

### ป้ายไตรมาสเขียนว่า `Q1'2026` — ไม่ต้องเปลี่ยนเป็น `2026'Q1`

รูปแบบนี้มาจากชีตต้นทางโดยตรง (`perCrop` คอลัมน์ Quarter) การกลับเป็น `2026'Q1`
เพื่อให้ "เรียงตามตัวอักษรแล้วถูก" เป็นการแก้ปลายเหตุ — และแก้ไม่ครบด้วย
เพราะป้ายเดือน (`2026-07`) กับป้ายวันยังต้องใช้กฎเรียงของตัวเองอยู่ดี

ทางแก้ที่ใช้จริงคือ **ทุกที่ที่เรียงป้ายเวลาต้องผ่าน `comparePeriod()`** ซึ่งตอนนี้ครบแล้ว:

| ที่ | ใครเรียง |
|---|---|
| ยอดรวมฝั่ง server (`byQuarter`, `byMonth`, `series`) | `comparePeriod()` ใน `shared/kpi.js` |
| ตัวเลือก "เก่า→ใหม่ / ใหม่→เก่า" บนการ์ดและ modal | `periodOrder()` ใน `ui/controls.js` |
| **หัวตารางที่กดเรียงได้** | `ui/table.js` ตรวจว่าทั้งคอลัมน์เป็นช่วงเวลาไหม แล้วสลับไปใช้ `comparePeriod()` |
| ชั้นตรวจซ้ำ | `verifyPresentation()` → finding `order.notChronological` |

หัวตารางเป็นจุดที่หลุดอยู่จุดเดียว (เคยได้ `Q1'2026 → Q2'2025`) แก้แล้วใน `isPeriodColumn()`
ซึ่งเข้าเงื่อนไขเมื่อ **ทุกค่าที่ไม่ว่างในคอลัมน์นั้น** เป็นช่วงเวลา — ต้องเป็นทุกค่า ไม่ใช่บางค่า
ไม่งั้นคอลัมน์ข้อความที่บังเอิญมีเลข 4 หลักปนบางแถวจะถูกเรียงด้วยกฎเวลาแล้วที่เหลือไปกองท้าย

### รูปแบบ finding

```js
{
  id, severity: 'critical'|'warning'|'info',
  source, tab, gid,          // ชีตต้นทาง + ชื่อ tab + gid ของ tab นั้น
  row, field,
  messageTh, messageEn,
  expected, actual, delta,
  related: [{ source, tab, gid }]   // ชีตอื่นที่ต้องเปิดดูประกอบ
}
```

`gid` ถูกเติมอัตโนมัติตอนท้ายของ `analyze()` โดยค้นจากชื่อ tab ใน `source.tabs`
front-end เอาไปทำลิงก์ `…/edit?gid=N#gid=N` ที่เปิดตรงไปยัง tab ที่มีปัญหาได้ทันที

`related` ใช้กับการตรวจข้ามรายงาน ซึ่งต้องเปิดดูมากกว่าหนึ่งชีตถึงจะเข้าใจปัญหา
เช่น `cross.shipmentMismatch` ต้องเทียบชีต "ขนย้ายออกจากฟาร์ม" กับ "รับดอกถึงกรุงเทพ"
จึงแนบลิงก์ทั้งสองฝั่งพร้อม tab ของวันนั้นมาให้ครบ

- `critical` — ตัวเลขขัดแย้งกันจนตัดสินใจผิดได้
- `warning` — ผิดหลักการแต่เดาเจตนาได้
- `info` — น่าสงสัยแต่อาจถูก

finding **ไม่บล็อกการแสดงผล** ข้อมูลยังขึ้นตามปกติ แต่ติดธงกำกับไว้

---

## 3. ทีม Agent

| Agent | หน้าที่ | ไฟล์ที่เป็นเจ้าของ |
|---|---|---|
| `data-analyst` | อ่าน แปลง และวิเคราะห์ความถูกต้องของข้อมูล | `server/lib/parsers/*`, `analysis.js`, `aggregate.js`, `normalize.js`, `tests/` |
| `ux-ui-designer` | ออกแบบ UX/UI, Glassmorphism, responsive, กราฟ, Three.js | `public/css/**`, `public/js/ui/**`, `public/js/charts/**`, `public/js/bg/**`, `index.html` |
| `backend-dev` | โครงสร้างเว็บ, API, cache, ความเสถียร | `server/server.js`, `server/lib/{csv,fetcher,cache}.js`, `scripts/sync-sources.js` |

นิยามอยู่ใน `.claude/agents/` เรียกใช้ผ่าน Task tool หรือพิมพ์ชื่อ agent

แต่ละ agent มี skill ประจำที่ต้องอ่านก่อนเริ่มงาน:
`data-analyst` → `.claude/skills/data-analysis/` · `ux-ui-designer` → `.claude/skills/responsive-ui/`

**การทำงานร่วมกัน:** `data-analyst` กำหนดว่าตัวเลขไหนเชื่อถือได้และแปลว่าอะไร →
`ux-ui-designer` กำหนดว่ามันหน้าตาอย่างไร → `backend-dev` กำหนดว่ามันเดินทางถึงเบราว์เซอร์อย่างไร
ห้าม agent หนึ่งแก้ไฟล์ของอีก agent โดยไม่แจ้ง

---

## 4. คำสั่ง

```bash
node scripts/sync-sources.js    # sync ลิงก์จาก .txt → config/sources.json
node server/server.js           # เปิดเซิร์ฟเวอร์ → http://localhost:5173
node --test tests/*.js          # smoke=parser · supply=วัสดุ · cost=งบ · ui=เบราว์เซอร์ · server=cooldown/ชุดสำรอง

# จัดการผู้ใช้ที่เข้า Dashboard ได้
node scripts/manage-users.js add <ชื่อผู้ใช้> --role exec   # ใช้ผู้ช่วย AI ได้
node scripts/manage-users.js add <ชื่อผู้ใช้> --role viewer # ดูรายงานอย่างเดียว
node scripts/manage-users.js list
node scripts/manage-users.js passwd <ชื่อผู้ใช้>
node scripts/manage-users.js remove <ชื่อผู้ใช้>
```

ไม่มี build step ไม่มี npm install ไม่มี dependency

### ตัวแปรสภาพแวดล้อม

อ่านจากไฟล์ `.env` ที่โฟลเดอร์โปรเจกต์ (gitignore แล้ว) หรือตั้งใน shell ก็ได้
ค่าที่ตั้งใน shell ชนะค่าในไฟล์เสมอ — ดูตัวอย่างที่ `.env.example`

| ตัวแปร | ค่าเริ่มต้น | ความหมาย |
|---|---|---|
| `GOOGLE_API_KEY` | — | คีย์ Google AI Studio สำหรับผู้ช่วย AI (ไม่ตั้ง = ปิดแชท) |
| `PORT` | `5173` | พอร์ต |
| `HOST` | `127.0.0.1` | ตั้งเป็น `0.0.0.0` เพื่อเปิดให้เครื่องอื่นเข้า |
| `SESSION_HOURS` | `12` | อายุ session หลังล็อกอิน |
| `CHAT_QUOTA_PER_DAY` | `50` | โควตาคำถามผู้ช่วย AI ต่อคนต่อวัน |
| `REFRESH_COOLDOWN_SEC` | `120` | คูลดาวน์ปุ่มรีเฟรช ใช้ร่วมกันทุกคน (ห้ามเกิน 300 = อายุ cache) |
| `TRUST_PROXY` | `0` | ตั้ง `1` **เฉพาะเมื่อมี** nginx/Caddy คั่นหน้าจริง ๆ |
| `ALLOW_NO_AUTH` | — | ตั้ง `1` เพื่อยอมเปิดสู่เครือข่ายทั้งที่ไม่มีล็อกอิน (ไม่แนะนำ) |
| `DEV_LOGIN` | — | ตั้ง `1` เพื่อให้พิมพ์อะไรก็ล็อกอินได้ (localhost เท่านั้น) |

---

## 5. โครงสร้างโปรเจกต์

```
kambis/
├── CLAUDE.md
├── แบบฟอร์มรายงาน Kambis.txt      ← แหล่งความจริงของลิงก์ (คนแก้เท่านั้น)
├── .claude/agents/               ← นิยาม agent 3 ตัว
├── .claude/skills/responsive-ui/ ← มาตรฐาน mobile/PC + เกณฑ์งานออกแบบ
├── .claude/skills/data-analysis/ ← มาตรฐานการอ่านไฟล์ดิบ + กฎการคำนวณ (+ scripts/read-any.mjs)
├── config/sources.json           ← GENERATED — ห้ามแก้มือ
├── config/users.json             ← ผู้ใช้ + hash รหัสผ่าน (gitignored, ห้าม commit)
├── .env                          ← API key (gitignored, ห้าม commit)
├── .env.example                  ← แม่แบบของ .env (commit ได้ ไม่มีค่าจริง)
├── scripts/
│   ├── sync-sources.js
│   └── manage-users.js           ← เพิ่ม/ลบ/เปลี่ยนรหัสผู้ใช้
├── server/
│   ├── server.js
│   └── lib/
│       ├── csv.js  fetcher.js  cache.js  normalize.js  env.js
│       ├── refresh-gate.js       ← คูลดาวน์ปุ่มรีเฟรช (กันยิง Google ถี่เกิน)
│       ├── tabs.js               ← ค้นรายชื่อแท็บของ Google Sheet
│       ├── auth.js               ← ล็อกอิน / session / จำกัดการเดารหัส
│       ├── gemini.js             ← เรียก Google AI Studio
│       ├── chat-context.js       ← ย่อข้อมูลเป็นบริบทให้ผู้ช่วย AI
│       ├── xlsx.js               ← เขียนไฟล์ Excel เอง (zero dependency)
│       ├── purchase-request.js   ← ออกใบขอซื้อวัสดุ
│       ├── parsers/{dailyTrim,perCrop,transfer,sales,inventory,supplyLog,cost}.js
│       │                           (transfer.js ใช้ทั้ง outbound และ inbound)
│       ├── analysis.js           ← Data Analysis engine
│       └── aggregate.js          ← ตัวส่งต่อไปยัง public/js/shared/kpi.js
├── public/
│   ├── index.html  login.html
│   ├── assets/  vendor/  css/
│   └── js/
│       ├── router.js             ← hash router ของสองรายงาน
│       ├── shared/{agg-core,kpi}.js  ← **สูตรรวมยอดที่ server ใช้ร่วมด้วย**
│       ├── pages/{overview,production,stock,sales,cost,supply}.js
│       ├── ui/{nav,filters,supply-filters,placeholder,gaps,notices,cards,modal,controls,table,loader,chat,icons}.js
│       │                           gaps.js = ทะเบียน "ข้อมูลที่ยังขาด" ของทั้งสองรายงาน
│       └── charts/  bg/
└── data/
    ├── cache/                    ← gitignored
    └── purchase-requests/        ← สำเนาใบขอซื้อที่เคยออก (gitignored)
```

---

## 6. แหล่งข้อมูล 8 รายงาน

| key | รายงาน | tabs | ลักษณะข้อมูล |
|---|---|---|---|
| `dailyTrim` | แบบฟอร์มน้ำหนักดอกทริมรายวัน | ~22 (ครอปละ tab) | วันที่ × สายพันธุ์ × ขนาด + ของไม่ใช่ดอก, มีแถว Total |
| `perCrop` | แบบฟอร์มน้ำหนักดอกทริมต่อครอป | ~34 (+ `SUMMARY SHEET`) | ไตรมาส → ครอป → รอบปลูก (Clone/Veg/Flower/Harvest/Dry) + ผลผลิต + จำนวนต้น + g/ต้น |
| `outbound` | ขนย้ายออกจากฟาร์ม | ~28 (ตามวันขน) | ครอป × สายพันธุ์ × ขนาด (กรัม) |
| `inbound` | รับดอกถึงกรุงเทพ | ~22 (ตามวันรับ) | โครงเดียวกับ outbound |
| `sales` | แบบฟอร์มขายดอก | 5 (Feb-March…July) | วันที่ × ลูกค้า × ครอป × สายพันธุ์ × ขนาด — **ไม่มีคอลัมน์ราคา** |
| `inventory` | สินค้าคงเหลือ | 3 — อ่าน 2 (`… Stock หัวหิน`, `… Stock กรุงเทพ`) · แท็บ `ส่งของให้ ลค.` ถูกข้ามพร้อม warning | สายพันธุ์ × ขนาด ณ วันที่อัปเดต (ภาพนิ่ง เขียนทับทุกครั้ง) |

### `inventory` เป็น parser เดียวที่ผูกกับ "ชื่อแท็บ" — เคยพังมาแล้ว

`STOCK_TAB_RE` ใน `parsers/inventory.js` คัดว่าแท็บไหนคือภาพสต็อก
parser อื่นไม่ผูกกับชื่อแบบนี้ (`perCrop` ใช้ `/summary\s*sheet/i` แบบไม่ anchor
ส่วน transfer ใช้ชื่อเป็น "รายการที่ต้องข้าม" ซึ่งพลาดแล้วข้อมูลไม่หาย)

**เคสจริง ส.ค. 69:** มีคนเติมเลขลำดับเอกสารหน้าชื่อแท็บเป็น `9.58 Stock หัวหิน`
ตามธรรมเนียมที่ชีต Log Stock ใช้อยู่แล้ว (`1.Rockwool` — 126 จาก 139 แท็บ)
regex เดิมบังคับ `^stock` สต็อกทั้งสองคลังจึงถูกข้ามหมด **เหลือ 0 แถว**
แต่หน้าเว็บยังโชว์ `0 kg` เฉย ๆ เหมือนเป็นคำตอบจริง

กฎที่ต้องรักษาไว้:

- ยอมให้มีเลข/วงเล็บ/ขีด คั่นหน้าคำว่า `stock` ได้ เพราะเป็นธรรมเนียมของบริษัท
- **ห้ามผ่อนเป็น `/stock/i` เด็ดขาด** แท็บ `ส่งของให้ ลค.  Stock กรุงเทพ` มีคำว่า
  Stock อยู่กลางชื่อ แต่เป็นของที่**ส่งออกไปแล้ว** ไม่ใช่ของคงเหลือ
  รับเข้ามานับจะทำให้สต็อกเกินจริง — แยกด้วยโครงสร้างก็ไม่ได้เพราะหัวตารางเหมือนกันเป๊ะ
- ชื่อคลังอ่านจาก **เนื้อในชีต** (`readLocationHeader()` แถวที่ 2) ไม่ใช่จากชื่อแท็บ
  ชื่อแท็บเป็นทางสำรองเท่านั้น และต้องตัดเลขลำดับออกก่อน

มี unit test ของ regex ตรง ๆ ใน `tests/smoke.js` — จับได้แม้ snapshot จะเป็นของเก่า
| `supplyLog` | Log Stock บันทึกประจำวัน | **139** (1 สรุป + 12 ปุ๋ย + 126 รายการ) | วัสดุสิ้นเปลือง: วันที่ × รับ/เบิก/คงเหลือ/ขั้นต่ำ **(lazy)** |
| `cost` | แบบฟอร์มต้นทุน | 6 | **งบรายรับ-รายจ่ายรายเดือน** — รายได้ · ต้นทุน · EBITDA · ค่าเสื่อม · EBIT |

### `cost` — ชีตเดียวในระบบที่มี "รายได้"

ลิงก์ที่ 8 เพิ่งเข้ามา และเป็นชีตแรกที่มีตัวเลขเงินระดับบริษัท
ก่อนหน้านี้ทุกช่องที่ต้องใช้เงินเป็น "รอข้อมูล" ถาวร ตอนนี้เติมได้แล้ว
ติด `kind: 'finance'` ให้ `analysis.js` ข้ามกฎน้ำหนัก/ขนาด/สายพันธุ์ แล้วใช้ `checkFinance()` แทน
และ **ไม่นับเข้าตัวหารคะแนนคุณภาพ** ด้วยเหตุผลเดียวกับ `supplyLog`

| แท็บ | ใช้ทำอะไร |
|---|---|
| `สรุป` | **ตัวเลขที่เชื่อถือได้** — Revenue · ต้นทุนวัตถุดิบ/Farm/Office · EBITDA · ค่าเสื่อม · EBIT |
| `ต้นทุน` · `Farm` · `Office` | รายการรายเดือน ใช้ทำ "อันดับรายการที่ใช้เงินสูงสุด" เท่านั้น |
| `ค่าเสื่อมราคา` | ทะเบียนสินทรัพย์ 326 แถว — `detailOnly` เพราะยอดรายเดือนอยู่ในแท็บสรุปแล้ว |
| `ต้นทุน ต่อ กรัม 2026` | **ตอนนี้ยังเป็นสำเนาของแท็บ Office ทั้งแท็บ** ถูกข้ามอัตโนมัติ |

**สี่กับดักที่ทำให้อ่านตรง ๆ ไม่ได้**

14. **หัวคอลัมน์เดือนที่หกถูกชื่อรายงานที่ merge ทับ** ทุกแท็บมีเซลล์
    `"รายงานค่าใช้จ่ายจาก Office …"` วางทับหัว `Jun-26` พอดี แต่ข้อมูลใต้คอลัมน์นั้น
    เป็นของเดือนมิถุนายนจริง → **หาคอลัมน์เดือนจากตำแหน่ง (`Jan-26` + 11 ช่อง) ไม่ใช่จากข้อความหัว**
    แล้วยืนยันด้วยผลรวม 12 เดือน = ช่อง Total
14.5. **แถวยอดรวมบางแถวขึ้นต้นด้วยเลขข้อ ไม่ใช่คำว่า "รวม"** — แท็บ `ต้นทุน` วางยอดรวม
    ไว้ **ใต้** ลูกของมันและเขียนว่า `1) รวม ค่าบุคลากร` · `2) รวมปุ๋ย อะธีน่า`
    กฎ `/^(รวม|total)/` จึงจับไม่ได้ ทำให้ยอดรวมไปเรียงแข่งกับลูกตัวเองในกราฟ
    **เกินจริง 6.85 ล้าน** และ finding รายงานส่วนต่างเป็น 6.88 ล้านแทนที่จะเป็น 26,672
    → `buildCostBreakdown()` ใน `shared/kpi.js` แยกกลุ่มจากโครงชีต ใช้ร่วมกับ `analysis.js`

    | แถว | แปลว่า |
    |---|---|
    | ขึ้นต้นด้วยเลขข้อ (`1)` `4.1)` `5)`) | **หัวข้อต้นทุนการปลูก** |
    | ไม่มีเลขข้อ อยู่ *ก่อน* หัวข้อ | ลูกของหัวข้อนั้น (ห้ามนับซ้ำกับยอดรวม) |
    | ไม่มีเลขข้อ อยู่ *หลัง* หัวข้อสุดท้าย | **ต้นทุนเบ็ดเตล็ด** |

    อ่านจากโครงของชีตเอง **ไม่ฮาร์ดโค้ดรายชื่อ** เพิ่ม `6)` ในชีตแล้วขึ้นเองอัตโนมัติ
    และ **รายการย่อยต้องตัดที่ `lastActiveMonth` เหมือนยอดรวมด้านบน** ไม่งั้นกราฟจะบวก
    ครบ 12 เดือนอยู่ใต้ช่องที่เขียนว่า "ม.ค.–ก.ค." (ต่างกัน 154,500 จากค่า Office ที่ตั้งล่วงหน้า)

15. **แถวยอดรวมปนอยู่กับแถวรายการ** (`รวม`, `Total`, `รวมต้นทุนการปลูก`) และมี
    **บรรทัดหมายเหตุใต้ยอดรวมใหญ่** (`- ค่าเบ็ดเตล็ด Office` 1.25 ล้าน) ถ้านับเป็นรายการ
    ยอดจะเกินเท่าตัว → ตัดแถวยอดรวม และตัดทุกแถวที่อยู่ **หลังยอดรวมใหญ่** ของแท็บรายละเอียด
    (แท็บสรุปห้ามตัด เพราะ EBITDA/ค่าเสื่อม/EBIT อยู่ใต้แถวรวม)
16. **แท็บที่เนื้อหาซ้ำกันทั้งแท็บ** จับด้วยลายเซ็นเนื้อหา ไม่ใช่ชื่อ และให้แท็บที่
    **ชื่อตรงกับหน้าที่** ได้อ่านก่อนเสมอ (`classifyTab()` มี `priority`) ไม่งั้น
    `ต้นทุน ต่อ กรัม 2026` จะชิงอ่านก่อนแล้วค่าใช้จ่าย Office ไปโผล่ในต้นทุนวัตถุดิบ
17. **งบสรุปยังไม่ตรงกับผลรวมรายการอยู่สามจุด** (ต้นทุนวัตถุดิบต่าง 906,854 บาท
    เพราะสูตร Total ของแถวค่าไฟฟ้าไม่ครอบเดือนกรกฎาคม) → **ยอดบน Dashboard ใช้แท็บสรุปเสมอ**
    เพราะเป็นตัวเลขที่ผู้บริหารถืออยู่จริง ส่วนความต่างออกเป็น finding `finance.summaryMismatch`
    (critical) **ห้ามเลือกข้างหรือแก้ให้ตรงกันเอง**

กฎตรวจของ `checkFinance()`:

| finding | ตรวจอะไร | ระดับ |
|---|---|---|
| `finance.summaryMismatch` | งบสรุป vs ผลรวมรายการในแท็บรายละเอียด | critical |
| `finance.ebitdaMismatch` | **EBITDA ต้องเท่ากับ รายได้ − รวมต้นทุนการปลูก** | critical |
| `finance.ebitMismatch` | EBIT ต้องเท่ากับ EBITDA − ค่าเสื่อมราคา | warning |
| `finance.rowTotalMismatch` | Σ 12 เดือนของแถว vs ช่อง Total ของแถวนั้น | warning |
| `finance.coverageMismatch` | บรรทัดที่กรอกครอบคลุมเดือนไม่เท่ากัน | warning |
| `finance.duplicateTab` · `finance.noSummary` | แท็บซ้ำ · อ่านแท็บสรุปไม่ได้ | warning · critical |

**เคสจริงที่ `ebitdaMismatch` จับได้:** มีคนแก้แถวต้นทุนในงบสรุปให้ตรงกับแท็บรายละเอียด
(เพิ่มขึ้นรวม 1,227,977 บาท) แต่ **ไม่ได้คำนวณแถว EBITDA กับ EBIT ใหม่** ช่องพวกนั้นจึงค้างค่าเก่า
→ Dashboard คิดกำไรขั้นต้นใหม่จาก รายได้ − ต้นทุน เสมอ **ห้ามอ่านช่อง EBITDA มาแสดงตรง ๆ**

### ยอดรวมต้องตัดที่เดือนที่เกิดขึ้นจริง

แต่ละบรรทัดในงบกรอกมาไม่เท่ากัน — รายได้ 6 เดือน · ต้นทุนวัตถุดิบ 7 เดือน ·
**ค่าเสื่อมราคากับค่าใช้จ่าย Office ตั้งไว้ล่วงหน้าครบ 12 เดือน**

ถ้าบวกทั้ง 12 เดือนจะกลายเป็นเอารายได้ 6 เดือนไปหักค่าเสื่อม 12 เดือน แล้วได้ผลขาดทุน
เกินจริงไป 4.3 ล้าน `buildCost()` จึงตัด `totals` ที่ `lastActiveMonth` แล้วเก็บยอด 12 เดือน
ไว้ที่ `totalsFullYear` (ไม่ได้ซ่อน — ตารางรายเดือนยังโชว์ครบ) และ **ป้ายบนการ์ดต้องบอกช่วงเวลาจริง
ห้ามเขียนแค่ปี**

ป้ายนั้นสร้างจาก `costSpan()` ใน `pages/shared.js` **ตัวเดียวใช้ทั้งหน้าภาพรวมและหน้าต้นทุน**
(`ม.ค.–ก.ค. 2026`) และ **เขียนปีเป็น ค.ศ. ไม่ใช่ พ.ศ.** ต่างจาก `format.month()` ที่ใช้ locale ไทย
แล้วได้ `ก.ค. 69` — เพราะป้ายนี้อยู่ใต้แถบตัวกรองที่เขียนว่า "2026" ถ้าคนละศักราชกัน
ผู้ใช้จะไม่แน่ใจว่ากำลังดูปีไหน

### `buildCost(source, year)` — "ชีตล่ม" กับ "ปีนี้ไม่มีข้อมูล" คนละเรื่องกัน

ตัวกรองปีส่งถึง `buildCost()` โดยตรง ไม่ผ่าน `applyFilters` (เหตุผลอยู่ใน §6.5)
**ฝั่ง server ไม่ส่งปี → ได้ผลเหมือนเดิมทุกตัวอักษร** จึงไม่กระทบ `verifyPresentation`

| ฟิลด์ | ความหมาย |
|---|---|
| `available` | มีตัวเลขให้แสดงไหม (ของปีที่เลือก) |
| `sheetAvailable` | **ชีตอ่านได้ไหม** — ใช้แยกสองสถานะนี้ |
| `requestedYear` · `years` | ปีที่ขอ · ปีที่ชีตมีจริง (ใช้บอกผู้ใช้ว่ามีปีไหนให้ดู) |

ทุกที่ที่ถามว่า "ชีตต้นทุนใช้ได้ไหม" ต้องดู `sheetAvailable` (`gaps.js` · การ์ดรอข้อมูลบนหน้าภาพรวม ·
หน้าต้นทุน) **ถ้าเผลอใช้ `available` เลือกปีที่ชีตไม่มีข้อมูลจะขึ้นว่า "ดึงชีตไม่สำเร็จ"
ทั้งที่ชีตปกติดี แล้วคนจะไปไล่แก้ชีตที่ไม่ได้ผิด**

**ต้นทุนต่อกรัมยังทำไม่ได้** ถึงจะมีทั้งต้นทุนรายเดือนและผลผลิตรายเดือน เพราะต้นทุน
ของเดือนหนึ่งเป็นของครอปที่เก็บเกี่ยวอีกเดือนหนึ่ง — ต้องให้คนกำหนดกติกาผูกก่อน
ห้ามหารกันตรง ๆ (มีไว้ในทะเบียน `gaps.js` แล้ว และ SYSTEM_PROMPT ของแชทห้ามไว้ด้วย)

### `supplyLog` ต่างจากรายงานอื่นทุกข้อ

รายงานนี้ไม่เกี่ยวกับดอกไม้เลย จึงติด `kind: 'supply'` ไว้ใน `config/sources.json`
แล้ว `analysis.js` ข้ามกฎที่เป็นเรื่องน้ำหนัก/ขนาด/สายพันธุ์ทั้งหมด ใช้ `checkSupply()` แทน

| เรื่อง | ทำไมต้องต่าง |
|---|---|
| `lazy: true` | 139 แท็บ ถ้าโหลดพร้อมกันหน้า Dryflower จะช้าขึ้นเกือบเท่าตัวทั้งที่ไม่ได้ใช้ |
| **ดึงต่อในเบื้องหลัง** | ไม่ได้อยู่ใน `/api/reports` แต่ `main.js` ยิงตามทันทีที่หน้าแรกวาดเสร็จ |
| `tabConcurrency: 10` | แท็บเล็กมาก ดึงพร้อมกันได้เยอะกว่าชีตดอกไม้ (~8 วิ แทน ~35 วิ) |
| **ไม่นับเข้าคะแนนคุณภาพ** | มี log หลายพันแถว ถ้านับเข้าตัวหาร คะแนนจะพุ่งขึ้นทั้งที่ข้อมูลไม่ได้ดีขึ้น |
| ตัด `rows` ออกจาก response | record มาตรฐานมีช่องของดอกไม้ที่เป็น null 16 ช่อง — 5,000 แถวคือ 2.8 MB ของค่าว่าง ส่งขึ้นเบราว์เซอร์เป็น `kpi.items[].log` แบบย่อแทน |

#### แถบตัวกรองของหน้า Supply

`public/js/ui/supply-filters.js` — 4 ตัว: ปี · ค้นหารายการ · หมวด · ราคา
กางไว้ทุกช่อง ไม่ซ่อนใน popup แบบรายงาน Dryflower เพราะเป็นช่องเดี่ยวทั้งหมด

- **หน้าเป็นคนวาดใหม่เอง ไม่ผ่าน router** ใช้ `setParams(patch, { silent: true })`
  ซึ่งอัปเดต URL แต่ไม่ยิง event — ถ้าให้ router วาดทั้งหน้าใหม่ **โฟกัสจะหลุดจาก
  ช่องค้นหาทันทีที่พิมพ์ตัวแรก** (ช่องค้นหาหน่วง 220ms ก่อนกรองด้วย)
- แถบตัวกรองกับการ์ดคุณภาพข้อมูลอยู่ **นอก** กล่องที่ถูกวาดใหม่
  การ์ดคุณภาพข้อมูลเป็นคุณภาพของ "ทั้งชีต" ไม่ใช่ของที่กรองไว้
- ตารางสั่งของรายเดือนใช้ชื่อรายการคนละแบบกับชื่อแท็บ ตอนกรองหมวดจึงต้องหาหมวด
  จาก `matchedOrderRow` ด้วย (`supplyLookup()`) ไม่งั้นกรองหมวดแล้วตารางมูลค่าจะว่างทั้งตาราง

#### ตาราง "รายการสต๊อกปัจจุบัน" — คงเหลือมาจาก log ไม่ใช่ตารางสั่งซื้อ

แถวมาจาก **แท็บ log ของแต่ละรายการ (138 แท็บ)** ไม่ใช่ตารางสั่งซื้อรายเดือน (60 แถว)
`คงเหลือ` จึงเป็นยอดจริง ณ วันนี้ที่ parser อ่านจากแถวล่าสุดที่วันที่ ≤ วันนี้ให้แล้ว
ส่วนช่อง "คงเหลือ" ในตารางสั่งซื้อเป็นเลขที่คนพิมพ์ไว้เอง คนละตัวกัน

- **มูลค่า = คงเหลือ × ราคา/หน่วย** (มูลค่าของสต๊อกที่ถืออยู่) ต่างจากของเดิมที่เป็น
  จำนวนที่จะสั่ง × ราคา (มูลค่าของแผนสั่งซื้อ) — ช่องตัวเลข "มูลค่าตามตารางสั่งซื้อ"
  บนแถบด้านบนยังเป็นอันหลังอยู่ ป้ายจึงต้องบอกให้ชัดว่าเป็นคนละตัว
- **รายการที่จับคู่ราคาไม่ได้ต้องขึ้น `*ยังไม่ใส่ราคา` สีแดง** (`.cell-missing`)
  **ห้ามเดาราคาหรือคิดเป็น 0** ไม่งั้นมูลค่ารวมของสต๊อกจะต่ำกว่าจริงโดยไม่มีอะไรบอก
  (`.cell-missing` ใช้ `--sev-critical` เพราะความหมายเดียวกับ finding: ต้องไปเติมที่ชีต
  ไม่ใช่ `--money-neg` ที่แปลว่าขาดทุน)

#### "รีเฟรชข้อมูล" ต้องรีเฟรชรายงานนี้ด้วย

ปุ่มรีเฟรชหมายถึง **ทุกรายงาน** ไม่ใช่เฉพาะรายงานดอก ไม่งั้นผู้ใช้ที่กดรีเฟรชแล้วเข้าหน้า
Supply ยังต้องรอโหลดอีกรอบอยู่ดี — `load()` ใน `main.js` จึงเรียก
`requestSupply({ force: refresh, quiet: true })` ต่อท้าย

- **ห้าม `await`** ตรงนั้น Dashboard หลักวาดเสร็จและ loading screen ปิดไปแล้ว
  ถ้ารอ ผู้ใช้จะเห็นหน้าจอโหลดนานขึ้นอีก 8 วินาทีเพื่อข้อมูลที่หน้าปัจจุบันไม่ได้ใช้
- `quiet: true` = วาดหน้าใหม่เฉพาะเมื่อหน้าที่เปิดอยู่ใช้ข้อมูลชุดนี้จริง
  (หน้า Supply · หน้าต้นทุน — ดู `usesSupply()` ใน `main.js`) ไม่งั้นหน้าที่ผู้ใช้
  กำลังอ่านอยู่จะกระตุกโดยไม่มีอะไรเปลี่ยน
  **หน้าภาพรวมเคยอยู่ในลิสต์นี้เพราะมีช่อง "ต้นทุนวัสดุสิ้นเปลือง" — ช่องนั้นถูกเอาออกแล้ว
  ตามคำสั่งผู้ใช้ ถ้าเพิ่มช่องที่ใช้ข้อมูล Supply กลับเข้าหน้าไหน ต้องเติมหน้านั้นกลับเข้า `usesSupply()` ด้วย**
- ผลที่ได้: กดเข้าหน้า Supply ได้เลยโดยไม่เจอหน้าจอ "กำลังโหลด"
  ตราบใดที่ยังไม่เข้าไปเร็วกว่าที่มันโหลดเสร็จ

### การจับการเบิกที่ผิดปกติ (`findUsageAnomalies()` ใน `shared/kpi.js`)

เทียบอัตราการเบิกของเดือนล่าสุดกับค่าเฉลี่ยของเดือนก่อน ๆ เพื่อบอกว่ารายการไหนเบิกพุ่งผิดปกติ

**กับดักที่ต้องระวัง: เดือนล่าสุดยังไม่จบ** ถ้าเอายอดรวม 3 วันแรกของเดือนไปเทียบกับ
เดือนเต็ม ๆ ที่ผ่านมา **ทุกรายการจะดู "ต่ำผิดปกติ" หมดตั้งแต่ต้นเดือน** จึงเทียบเป็น
**อัตราต่อวัน** (เดือนก่อนคิดที่ 30 วัน เดือนนี้คิดตามวันที่ผ่านมาจริง) และบอกบนหน้าจอ
ด้วยว่ากำลังเทียบเดือนไหนกับเดือนไหน ผ่านมากี่วันแล้ว — มี test คุมเคสนี้ไว้แล้ว

ต้องมีเดือนที่มีการเบิกจบแล้วอย่างน้อย 2 เดือนถึงจะเทียบ ถ้ายังไม่ครบ **ต้องบอกตรง ๆ ว่า
ข้อมูลยังไม่พอ ห้ามเดา** (ตอนนี้ชีตมีการเบิกจริงแค่เดือนเดียว หน้านี้จึงยังไม่ทำงาน
และจะเริ่มเองเมื่อข้อมูลครบ — ตั้งใจให้เป็นแบบนี้ ดีกว่าเตือนผิดจนคนเลิกเชื่อ)

---

## 6.5 โครงหน้าเว็บ (IA)

Dashboard แบ่งเป็นสองรายงาน ใช้ **hash router ในหน้าเดียว** (`public/js/router.js`)

| route | หน้า |
|---|---|
| `#/dryflower/overview` | ภาพรวมผู้บริหาร (ค่าเริ่มต้น — route ที่ไม่รู้จักมาที่นี่) |
| `#/dryflower/production` · `/stock` · `/sales` · `/cost` | หน้าย่อย 2–5 |
| `#/supply` | KAMBIS SUPPLY STOCK (หน้าเดียวจบ) |

ใช้ hash ไม่ใช่ History API เพราะส่วน hash ไม่เคยเดินทางถึงเซิร์ฟเวอร์ จึงรอดจาก
302 ของหน้า login (ที่ส่ง `?next=` จาก `pathname + search`) โดยไม่ต้องแตะ `serveStatic` เลย

### เมนู: จอใหญ่กับจอเล็กคนละแบบ

| จอ | เมนู |
|---|---|
| ≥ 768px | สองชั้น — `#report-nav` (2 รายงาน) บน header · `#subnav` (5 หน้า) ใต้ header |
| < 768px | ปุ่ม ☰ เปิดแผงเต็มจอ `#nav-panel` ที่เห็นทั้ง 7 ปลายทางพร้อมกัน |

**ห้ามกลับไปใช้แถบที่เลื่อนแนวนอนบนจอเล็ก** (`.subnav { overflow-x: auto }` แบบเดิม)
มันดูเหมือนแก้ปัญหาที่แคบ แต่จริง ๆ คือซ่อนหน้า 4–5 ไว้นอกจอโดยไม่มีอะไรบอกว่ามีอยู่
คนที่ไม่เคยใช้จะไม่มีวันรู้ว่ามีหน้า "ต้นทุน" — รายละเอียดทั้งหมดอยู่ใน skill `responsive-ui`

จุดที่พลาดง่ายใน `ui/nav.js`:

- **ลิงก์ทุกที่ใช้ `data-report` / `data-page` ชุดเดียวกัน** `renderNav()` จึงอัปเดต
  ทั้งเมนูบน เมนูย่อย และแผง ด้วย `document.querySelectorAll()` ครั้งเดียว ไม่มีทางไม่ตรงกัน
- **เครื่องมือใน header ถูก "ย้าย" ไม่ใช่ "ทำสำเนา"** (`placeTools()` ย้าย
  `#header-tools-meta` เข้า/ออกจากแผงตามความกว้างจอ) ถ้าทำสำเนาจะได้ id ซ้ำในหน้าเดียว
  แล้ว listener ที่ `main.js` ผูกไว้จะทำงานแค่ตัวเดียว
- **ปิดแผงเฉพาะตอน "เปลี่ยนหน้า" ไม่ใช่ทุกครั้งที่ `render()`** เพราะปุ่มธีม/ภาษาอยู่ในแผง
  ถ้าปิดทุกครั้งจะกลายเป็นกดเปลี่ยนธีมทีแผงหายที (เทียบด้วย `lastRouteKey`)
- **แผงอยู่ z-index 35 ใต้ header (40) โดยตั้งใจ** เพราะปุ่ม ☰ ที่กลายเป็น ✕ อยู่บน header
  ถ้าแผงทับ header จะปิดด้วยปุ่มเดิมไม่ได้ — และด้วยเหตุนี้ focus trap ต้องนับปุ่ม ☰
  ที่อยู่นอกแผงเข้าไปในวงด้วย จึงต้องดัก `keydown` ที่ `document` ไม่ใช่ที่ตัวแผง
- **หน้าย่อยทั้ง 5 โชว์ในแผงเสมอ** แม้ตอนอยู่รายงาน Supply (ต่างจาก `#subnav` บนจอใหญ่ที่ซ่อน)
  เพราะแผงคือเมนูรวมของทั้งเว็บ ถ้าซ่อนไว้จะต้องเปิดเมนูสองเที่ยวเพื่อข้ามรายงาน

### ลำดับ 5 ขั้นตอนสลับหน้า — ห้ามสลับลำดับ

```js
releaseCharts(el.page);   // 1. ก่อนทิ้ง DOM เสมอ ไม่งั้น ResizeObserver + canvas รั่วทุกครั้ง
el.page.innerHTML = '';   // 2.
page.render({ host, ... });// 3. สร้าง DOM (ยังลอยอยู่นอกหน้าจอ)
el.page.appendChild(host); // 4. เข้า DOM แล้ว — ตรงนี้เท่านั้นที่วัดความกว้างได้
runDeferred(drawLater);    // 5. ค่อยวาดกราฟ
```

- **ห้ามเก็บทุกหน้าไว้ใน DOM แล้วสลับด้วย `hidden`/`display:none`** —
  `setupCanvas()` คืน `null` เมื่อกว้าง 0 แล้วกราฟ bail เงียบ ๆ และ `onResize` ข้าม
  `width === 0` จึงไม่มีวันแก้ตัวเองตอนแสดงผล
- `renderCards()` ที่เรียกจากหน้าใด ๆ **ต้องส่ง `defer: drawLater`** ไม่งั้นมันจะวาดทันที
  ตอนที่กล่องยังไม่อยู่ในหน้า แล้วกราฟบนการ์ดหายหมด (เคยพังมาแล้ว)
- ถ้าซ่อนอะไรด้วย `hidden` แล้ว CSS ตั้ง `display` ให้คลาสนั้นด้วย
  **ต้องเขียน `.x[hidden]{display:none}` กำกับ** เพราะ specificity เท่ากันแต่คลาสมาทีหลัง
  (เมนู Dryflower เคยโผล่ค้างบนหน้า Supply เพราะเหตุนี้)

### แถบตัวกรองกลาง

`public/js/ui/filters.js` — 6 ตัว: **ปี** · ช่วงวันที่ · สถานที่ · ขนาดดอก · สายพันธุ์ · ครอป
สถานะเก็บใน hash (`#/dryflower/stock?loc=bangkok`) จึงส่งลิงก์ต่อและรีเฟรชได้

#### ตัวกรองปี — ค่าเริ่มต้นคือปีล่าสุด

ผู้ใช้กำหนดว่า **ทั้งสองรายงานต้องเปิดมาที่ปีล่าสุดที่มีข้อมูล** ไม่ใช่ยอดสะสมตั้งแต่เปิดฟาร์ม

| ค่าใน URL | ความหมาย |
|---|---|
| ไม่มี `year` | ยังไม่ได้เลือกเอง → **ปีล่าสุดที่มีข้อมูล** (`resolveYear()`) |
| `year=all` | ผู้ใช้เลือกดูย้อนหลังทั้งหมดเอง |
| `year=2025` | ปีนั้นปีเดียว |

- **`''` กับ `'all'` ต้องแยกจากกันเสมอ** ไม่งั้นพอกด "ล้างตัวกรอง" ตอนดูทุกปีอยู่
  มันจะเด้งกลับไปปีล่าสุดทันที (`writeFilters()` จึงเขียน `year=all` ลง URL ทั้งที่ตัวอื่นไม่เขียนค่า all)
- **ช่องเลือกปีอยู่บนแถบตลอดเวลา ไม่ซ่อนใน popup เหมือนตัวกรองอื่น** เพราะปีมีค่าอยู่เสมอ
  ถ้ามองไม่เห็น ผู้ใช้จะเข้าใจว่ายอดที่เห็นคือทั้งหมด
- **แถวที่ไม่ผูกกับปี ต้องไม่ถูกกรองทิ้ง** — `recordYear()` คืน `null` ให้สินค้าคงเหลือ
  (เป็นภาพนิ่งของ "ตอนนี้" ไม่มีวันที่) ใช้หลักเดียวกับตัวกรองช่วงวันที่
- รายการปีที่เลือกได้ต้องคิดจากข้อมูล **ทั้งชุด** ครั้งเดียว (`filterChoices` ใน `main.js`)
  ห้ามคิดใหม่จากข้อมูลที่กรองแล้ว ไม่งั้นเลือกปี 2026 แล้วปีอื่นจะหายจากช่องเลือก กลับไปไม่ได้อีก
- หน้า Supply ใช้พารามิเตอร์ `year` ตัวเดียวกัน แต่ resolve จากเดือนที่มีการเบิกจริงของตัวเอง
  (`yearPicker()` ใน `pages/supply.js`) และ **ยอดรวมของตารางต้องคิดใหม่จากเดือนที่แสดงอยู่**
  ห้ามใช้ `r.total` ที่ server รวมมา เพราะนั่นเป็นยอดตลอดกาล แถวจะรวมไม่ตรงกับคอลัมน์ที่เห็น

สิ่งที่เห็นตลอดเวลาเป็นแถวเดียวสูง ~38px: **ปุ่มเปิด popup + ชิปสรุป + ปุ่มล้าง**
ตัวเลือกทั้งหมดอยู่ใน popup เพราะลูกค้าไม่ต้องการให้ตัวกรองกินพื้นที่จอ

- **กรองที่ระดับ record เสมอ** ทุกหน้าคำนวณจาก `payload.sources[key].rows`
  ตัวกรองจึงเป็นแค่ `rows.filter()` — ถ้าไปกรองบน `kpi.*` ที่รวมมาแล้วจะกรองไม่ได้จริง
- **`kpi` ถูกคำนวณใหม่ในเบราว์เซอร์จากแถวที่กรองแล้ว ส่วน `analysis`/`meta` ยังเป็นของทั้งชีต**

  `renderActivePage()` ใน `main.js` เรียก `buildKpi(sources, analysis, { year })` แล้วประกอบเป็น
  `view = { ...payload, sources, kpi }` ส่งให้ทุกหน้า ทุกการ์ด และทุก modal (`viewKpi()` มี memo)
  **ห้ามหน้าไหนอ่าน `payload.kpi` ที่ server ส่งมาตรง ๆ** — เดิม `renderCards()` ทำแบบนั้น
  การ์ดทุกใบจึงเป็นยอดตลอดกาลนั่งอยู่ใต้กราฟที่กรองปีแล้ว (ช่องบอก 673 kg การ์ดบอก 1,530 kg
  ในหน้าจอเดียวกัน) ส่วนคุณภาพข้อมูลเป็นเรื่องของทั้งชีต จึงห้ามรัน `analyze()` ในเบราว์เซอร์
- **`filterSources()` แตะเฉพาะรายงาน `kind: 'flower'`** รายงานที่ไม่ใช่ข้อมูลดอก
  (`finance` · `supply`) ผ่านไปทั้งก้อน แล้วรับ **ปีอย่างเดียว** ผ่าน `buildKpi(…, { year })`

  แถวงบมี `date` (parser ใส่ `${month}-01` ให้) จึงผ่านตัวกรองปีได้ **แต่ตัวกรองสายพันธุ์
  จะเทียบ `strains.has('')` ได้ false ทุกแถว และตัวกรองขนาดตัดทิ้งเพราะ `flowerTotal` เป็น null**
  → เลือกสายพันธุ์เดียวแล้วงบทั้งหน้าต้นทุนกลายเป็นศูนย์เงียบ ๆ (มี test คุมไว้แล้ว)
  ด้วยเหตุผลเดียวกัน `filterOptions()` ก็ข้ามรายงานที่ไม่ใช่ดอก ไม่งั้นวันที่มีคนตั้งงบปีหน้า
  ล่วงหน้า ปีเริ่มต้นจะเด้งไปปีนั้นแล้ว Dashboard เปิดมาว่างเปล่า
- `rowCount` ของ source ที่กรองแล้วคือจำนวนแถวที่เห็นจริง ยอดทั้งชีตอยู่ที่ `rowCountAll`
  (modal แสดง `N / ทั้งหมด`) — ถ้าไม่แยก หัว modal จะขัดกับตัวนับของตารางที่อยู่ใต้มันเอง
- **ห้ามแก้ record เดิม** คัดลอกแบบตื้นแล้วคำนวณยอดใหม่ (payload ถูกใช้ซ้ำทุกครั้งที่สลับหน้า)
- `replaceParams()` ต้อง `emit()` เอง เพราะ `history.replaceState` ไม่ยิง `hashchange`
- **popup ต้องแขวนที่ `document.body` ไม่ใช่ใน `#filter-host`** เพราะทุกครั้งที่ติ๊กเลือก
  หน้าจะถูกวาดใหม่และกล่องนั้นถูกล้าง — popup จะปิดเองทุกครั้งที่เลือกจนใช้งานไม่ได้
  ตอนออกจากรายงาน Dryflower ต้องเรียก `closeFilterPopup()` เก็บกวาดเอง
- แถบถูกสร้างใหม่**เฉพาะเมื่อ URL เปลี่ยนจากทางอื่น** (กดย้อนกลับ / เปิดลิงก์) หรือสลับภาษา
  ไม่ใช่ทุกครั้งที่ค่าเปลี่ยน ไม่งั้นโฟกัสหลุดทันทีที่เลือก —
  เวลาไม่สร้างใหม่จะสั่ง `bar.__sync(filters)` ให้อัปเดตแค่ชิปกับปุ่ม **ห้ามแตะ popup ที่เปิดอยู่**
- ค่าเริ่มต้น `'all'` แสดงเป็น "ติ๊กครบทุกช่อง" แบบตัวกรองใน Excel และ **ไม่ติ๊กเลย = ทั้งหมด**
  (กันจอว่างเปล่าที่ไม่มีประโยชน์) มีข้อความกำกับใต้รายการเสมอ

### สูตรรวมยอดมีชุดเดียว ใช้ร่วมกันสองฝั่ง

`public/js/shared/agg-core.js` และ `public/js/shared/kpi.js` อยู่ใน `public/`
เพราะเบราว์เซอร์โหลดไฟล์นอก `public/` ไม่ได้ ส่วน server import จากพาธบนดิสก์ได้ตามปกติ

- `server/lib/aggregate.js` เป็นตัวส่งต่อบาง ๆ ไปยัง `shared/kpi.js`
- `server/lib/normalize.js` และ `public/js/ui/controls.js` import `comparePeriod`
  จาก `agg-core.js` — **สำเนาที่เคยมีสองชุดถูกลบแล้ว** ไม่ต้อง "แก้ที่หนึ่งแล้วแก้อีกที่" อีก
- ไฟล์ใน `shared/` ต้องเป็น ESM ล้วน **ห้าม import อะไรนอกกลุ่มตัวเอง ห้ามแตะ DOM
  ห้ามใช้โมดูลของ node** ไม่งั้นฝั่งใดฝั่งหนึ่งจะ import ไม่ได้

### รูปแบบ record มาตรฐาน (ทุก parser คืนหน้าตานี้)

```js
{
  date, crop, strain,
  sizes:     { XXL, XL, L, M, S, XS },
  nonFlower: { shake, shake2, sugarleaf, kief, dokPan, dokRon, sesDok },
  flowerTotal, nonFlowerTotal,          // คำนวณใหม่จาก sizes เสมอ
  source, tab, rowIndex,
  raw: { statedFlowerTotal, statedPct } // ค่าที่ชีตบอก เก็บไว้เทียบ
}
```

---

## 7. ข้อบกพร่องที่รู้แล้วในข้อมูลต้นทาง

ทั้งหมดนี้เป็นปัญหาใน **ชีตต้นทาง** ไม่ใช่บั๊กของ Dashboard — engine ต้องจับได้ทุกข้อ
ห้ามแก้ให้ตัวเลขดูดีขึ้น ให้รายงานตามจริง

1. `dailyTrim` คอลัมน์ % เพี้ยน — Shake % = 412.12 / 461.11 / 496.21, Sugarleaf = 1,320.00 ในช่อง %
2. `outbound` แถว Total เอา % มาบวกกัน → 203.84% / 103.10% / 193.06%
3. `perCrop` ครอป `G4/14FEB25`: Total = 77,405 แต่ ยอดน้ำหนักรวม = 67,405 (ต่างกัน 10,000 g)
4. `inbound` header เขียน `(Kg)` แต่ค่าจริงเป็นกรัม; tab ชื่อ `21/07/26` แต่ในชีตเขียน `21/06/26`
5. `inbound` มีคอลัมน์ `*XS*` ซ้ำกับ `XS`
6. `sales` หลายแถวมีน้ำหนักแต่ `Total Flower` ว่าง (เช่นแถว 02/03/2026)
7. `perCrop` ครอป `G1/3 - 17JUL26` วันที่ Veg = `7 Aug 28` (พิมพ์ผิด ควรเป็น 26)
8. เซลล์ merge หายเป็นค่าว่าง — `sales` มีวันที่/ลูกค้าเฉพาะแถวแรกของกลุ่ม,
   `Stock กรุงเทพ` หัวคอลัมน์ S/XS/Shake/Sugarleaf หาย → ต้อง forward-fill และ map ตามตำแหน่ง
   เทียบกับ header มาตรฐานของ `Stock หัวหิน`

### ชีต Log Stock (`supplyLog`) — สี่เรื่องที่ทำให้อ่านตรง ๆ ไม่ได้

9. **หัวตาราง merge และขึ้นบรรทัดใหม่ในเซลล์** ข้อความหัวคอลัมน์กระจายข้ามแถว 0–2
   และบางแท็บมีหัวตาราง 3 แถว → หาคอลัมน์ด้วย **ตำแหน่ง + ตรวจด้วยเลขคณิต**
   (`คงเหลือ = คงเหลือเดิม + รับ − เบิก` และ `Index = คงเหลือ − ขั้นต่ำ`)
   **ห้าม match ข้อความหัวคอลัมน์**
10. **วันที่เป็น พ.ศ.** (`01/07/2569`) — `parseSheetDate()` แปลงให้อยู่แล้ว
    ห้ามเขียนตัวแปลงวันที่ตัวที่สอง
11. **มีแถวลงวันที่ล่วงหน้า** ที่ยอดคงเหลือถูก carry forward ไว้แล้ว →
    "ยอดปัจจุบัน" ต้องอ่านจาก **แถวล่าสุดที่วันที่ ≤ วันนี้** ไม่ใช่แถวสุดท้ายของแท็บ
    และ **`ขั้นต่ำ` เปลี่ยนได้ระหว่างทาง** (เจอจริง: COCO 85→38, Cuts 11→5)
    จึงต้องอ่านจากแถวเดียวกันนั้น ไม่ใช่แถวแรก
    *(นี่คือจุดที่ผิดแล้วยังดูสมเหตุสมผลที่สุดในโปรเจกต์ — มี test คุมไว้แล้ว)*
12. **เกณฑ์ "ต้องสั่งซื้อ" คือ `Index ≤ 0`** (`Index = คงเหลือ − ขั้นต่ำ`)
    รวมของที่คงเหลือ **เท่าขั้นต่ำพอดี** เข้ามาด้วย เพราะระหว่างรอของตาม Lead Time
    (5–7 วันตามที่เขียนในชีต) ของจะถูกเบิกจนต่ำกว่าขั้นต่ำแน่นอน รอให้ติดลบก่อนค่อยสั่งก็สายแล้ว
    *(เดิมใช้ `< 0` ผู้ใช้เปลี่ยนเป็น `≤ 0` — ทำให้รายการเพิ่มจาก 34 เป็น 44)*
    Index ต้องคำนวณใหม่จาก คงเหลือ − ขั้นต่ำ เสมอ **ห้ามเชื่อช่อง Index ในชีต** เพราะคนพิมพ์ทับสูตรได้

12.5. **ระยะเวลารอของ (Lead Time) อยู่ในหัวตาราง ไม่ใช่คอลัมน์**
    คนเขียนแทรกไว้ในเซลล์หัวตารางที่ merge หลายบรรทัด เช่น
    `Rockwool (98 หลุม/แผง) ใช้ 14 แผง/crop Lead Time - 5 Days จะสั่งครั้งละ 2 ลัง`
    เขียนกัน 3 แบบต่างแค่ขีดกับตัวพิมพ์ใหญ่เล็ก (`Lead time 7 days` · `Lead Time - 5 Days` · `lead time 14 days`)
    `parseLeadTimeDays()` ใน `parsers/supplyLog.js` ดึงออกมา — **มีแค่ 65 จาก 138 แท็บ**
    ที่เหลือต้องเป็น `null` **ห้ามเดาเป็น 0 หรือค่าเริ่มต้น** เพราะเลขที่เดาจะถูกเอาไปคิดว่า
    ต้องสั่งล่วงหน้ากี่วันแล้วผิดเงียบ ๆ (ตอนนี้ Dashboard แสดงเป็นคอลัมน์เฉย ๆ ยังไม่เอาไปคำนวณต่อ)
13. **ตารางสั่งของรายเดือนมีแค่ 60 รายการ แต่มีแท็บ log 138 รายการ** →
    ของที่ต้องสั่งซื้อราวครึ่งหนึ่งจึงไม่มีราคา ขึ้น finding `supply.missingPrice`
    และชื่อรายการเขียนไม่ตรงกันสองที่ (`ป้ายแท็ก-สีน้ำเงิน` กับ `7.ป้ายแท็กสีน้ำเงิน`)
    ต้อง normalize ก่อนจับคู่ และ **ถ้าเข้าเค้าหลายรายการให้ถือว่าจับคู่ไม่ได้**
    (เดาผิดแล้วได้ราคาผิด แย่กว่าไม่มีราคา)

### สองรายงานนับ "ครอป" คนละหยาบละเอียด

`perCrop` รวมสองห้องเป็นรหัสเดียว (`G1/1&G1/3-17NOV25`) ส่วน `dailyTrim` แยกทีละห้อง
(`G1/1-17NOV25`) — เทียบสตริงตรง ๆ จึงไม่มีวันตรงกันเลย ต้องแตกรหัสรวมออกก่อน
(ดู `expandCrop()` ใน `public/js/pages/production.js`)

---

## 8. API

| Route | คืนอะไร |
|---|---|
| `GET /api/reports` | `{ meta, sources, kpi, analysis }` — 7 รายงาน (memory cache 5 นาที) · `supplyLog` แยกต่างหาก |
| `GET /api/reports?refresh=1` | บังคับดึงใหม่ + วิเคราะห์ใหม่ — **อยู่ใต้คูลดาวน์ร่วม** |
| `GET /api/reports/:key` | รายงานเดียวแบบละเอียด |
| `GET /api/reports/supplyLog` | วัสดุสิ้นเปลือง — **โหลดแยก (lazy) cache 15 นาที** ตัด `rows` ดิบออก · เบราว์เซอร์ยิงเองในเบื้องหลังหลังหน้าแรกวาดเสร็จ |
| `POST /api/supply/purchase-request` | `{items:[{item,qty}]}` → ไฟล์ `.xlsx` ให้ดาวน์โหลด + เก็บสำเนาที่ `data/purchase-requests/` |
| `GET /api/analysis` | ผลวิเคราะห์อย่างเดียว |
| `GET /api/progress` | SSE ความคืบหน้าการโหลด (ขับ loading screen) |
| `GET /api/health` | uptime, อายุ cache, คะแนนคุณภาพข้อมูล, `dataHealth`, `degraded` |
| `GET /api/auth/status` | เปิดระบบล็อกอินไหม + ใครล็อกอินอยู่ (เข้าได้โดยไม่ต้องล็อกอิน) |
| `POST /api/auth/login` | `{username, password}` → ตั้งคุกกี้ session |
| `POST /api/auth/logout` | ล้างคุกกี้ |
| `GET /api/auth/me` | ข้อมูลผู้ใช้ปัจจุบัน + โควตาแชทที่เหลือ |
| `GET /api/chat/models` | รายชื่อโมเดล Gemini ที่เลือกได้ + `canChat` |
| `POST /api/chat` | ถามผู้ช่วย AI |
| `GET /api/usage` | สถิติ token สะสมตั้งแต่เปิดเซิร์ฟเวอร์ |

`meta.sources[].status` = `ok` | `stale` (ใช้ cache เพราะดึงไม่ได้) | `error`
แหล่งข้อมูลพังหนึ่งอันต้องไม่ทำให้ทั้ง response พัง

### กันข้อมูลเสียทับข้อมูลดี — `meta.health`

> **`status` ไม่พอที่จะบอกว่าข้อมูลใช้ได้** มันคำนวณจากผล HTTP กับ parser throw เท่านั้น
> ถ้า Google ตอบ 200 พร้อมหน้า login หรือ CSV ว่าง จะได้ `status: 'ok'` ที่ **0 แถว**

`payloadHealth()` ใน `loader.js` ตัดสินจาก `rowCount` เป็นหลัก แล้วติดผลไว้ที่ `meta.health`

| level | เสิร์ฟอะไร | เก็บใน RAM | เขียนชุดสำรอง |
|---|---|---|---|
| `good` | ชุดใหม่ | ✔ | ✔ |
| `partial` | ชุดใหม่ (บางรายงานพัง) | ✔ | ✘ |
| `bad` | **ชุดที่ดีล่าสุด** + `degraded: true` + `failedSources[]` | ✔ | ✘ |

- **ห้ามเรียก `writeSnapshot()` ตรง ๆ** ให้ผ่าน `keepSnapshot()` เสมอ ทั้งใน `loadAll` และ `loadLazySource`
  ไม่งั้น Google ล่มครั้งเดียวตอนผู้บริหารกดรีเฟรช = ชุดสำรองที่ดีหายถาวร รีสตาร์ทก็ไม่กลับมา
- `writeSnapshot` เขียนแบบ atomic (tmp → rename) และเก็บชุดก่อนหน้าไว้ที่ `.prev`
  ส่วน `readSnapshot` ตกไปใช้ `.prev` เองพร้อม**ตรวจรูปร่าง** (`meta` + `sources`/`source`)
  ไม่ใช่แค่ `JSON.parse` ผ่าน
- ตอนเสิร์ฟชุดสำรอง `meta.health` ถูกแทนด้วยผลของ **รอบที่เพิ่งพยายาม** ไม่ใช่ของชุดสำรอง
  เพื่อให้ `/api/health` รายงาน `dataHealth: 'bad'` ให้ตัวเฝ้าระวังภายนอกเห็นว่าตอนนี้ดึงไม่ได้

### คูลดาวน์ปุ่มรีเฟรช

`server/lib/refresh-gate.js` — 2 นาที **ใช้ร่วมกันทุกคนทุก role** แยก scope ระหว่าง
`reports` (125 คำขอ) กับ `supplyLog` (140 คำขอ)

- **กดก่อนครบเวลาได้ 200 พร้อมชุดล่าสุด ไม่ใช่ 429** เพราะ `getJson()` ฝั่งเบราว์เซอร์โยน error
  ทุกสถานะที่ไม่ใช่ 2xx แล้วเด้งไปหน้าจอ "โหลดไม่สำเร็จ" เต็มจอ ผู้บริหารจะอ่านว่าระบบพัง
  สถานะจริงอยู่ใน `meta.refresh = { requested, applied, waitMs, cooldownMs }` แล้วแถบเตือนบอกเวลาที่เหลือ
- **`gate.note()` เรียกก่อนเริ่มโหลด** ถ้าจดตอนโหลดสำเร็จ เวลา Google ล่มคูลดาวน์จะไม่เคยเริ่มนับ
  แล้วทุกคนจะยิงซ้ำรัว ๆ ตอนที่ปลายทางกำลังแย่ที่สุด
- **`REFRESH_COOLDOWN_SEC` ต้อง ≤ 300** (อายุ cache) ไม่งั้นคำขอที่โดนบล็อกจะไปเจอแคชหมดอายุ
  แล้วโหลดเต็มรอบอยู่ดี — โค้ดบังคับด้วย `Math.min()` ให้แล้ว
- เป็นคูลดาวน์ต่อเซิร์ฟเวอร์ ไม่ใช่โควตารายคน เพราะสิ่งที่ปกป้องคือโควตาฝั่ง Google ที่ใช้ร่วมกัน
  (โควตารายคนไม่คุมโหลดรวม: 10 คน × 5 ครั้ง = 13,250 คำขอ/วัน)

`meta.tabChanges[]` = แท็บที่เพิ่ม/ลบ/เปลี่ยนชื่อในรอบนี้ (front-end เอาไปขึ้นแถบแจ้งเตือน)

`meta.unmatchedLabels[]` = ลิงก์ในไฟล์ .txt ที่ยังไม่มี PROFILE รองรับ —
front-end ขึ้นแถบแจ้ง ไม่งั้นรายงานที่คนเพิ่มเข้ามาใหม่จะหายเงียบ

**HTTP method:** รับ `POST` เฉพาะ `/api/chat`, `/api/auth/login`, `/api/auth/logout`,
`/api/supply/purchase-request` ที่เหลือเป็น `GET`/`HEAD` อย่างเดียว
(ลืมเพิ่มเข้า `POST_ROUTES` แล้วจะได้ **405** ทันที)

### ใบขอซื้อ (.xlsx) — เขียนเองทั้งหมด

`.xlsx` คือ ZIP ที่บรรจุ XML และ Node มี `node:zlib` เป็น core module (ไม่ใช่ npm package)
จึงเขียนเองได้โดยไม่ผิดกฎ zero dependencies

- `server/lib/xlsx.js` — CRC32 + ZIP writer (local header / central directory / EOCD) + `deflateRawSync`
- ข้อความไทยเขียนเป็น **inline string** (`t="inlineStr"`) ไม่ใช้ `sharedStrings.xml`
- `server/lib/purchase-request.js` — ออกเลขที่ `PR-YYYYMMDD-NNN`, ตรวจข้อมูลที่ส่งมา, เก็บสำเนา
- **ราคาเอาจากชีตเสมอ ไม่เอาที่เบราว์เซอร์ส่งมา** ไม่งั้นใครก็แก้ราคาในใบขอซื้อได้จากฝั่ง client
- ของที่ไม่มีราคาในชีต ยอดต้องเป็นค่าว่าง **ห้ามคิดเป็น 0** เพราะยอดรวมจะต่ำกว่าจริง

---

## 8.5 ระบบล็อกอิน

> ออกแบบมาเพื่อให้เอาขึ้นเซิร์ฟเวอร์แล้วเปิดให้ผู้บริหารเข้าผ่าน domain ได้

**เปิดใช้งานเมื่อมีไฟล์ `config/users.json` ที่มีผู้ใช้อย่างน้อย 1 คน**
ถ้าไม่มีไฟล์ = ไม่มีล็อกอิน (โหมดรันบนเครื่องตัวเอง)

### ด่านตรวจ

`server.js` กั้น **ก่อน** ทุก route โดยตั้งใจ — endpoint ใหม่ที่เพิ่มทีหลังจะถูกกั้นอัตโนมัติ
ทางที่เปิดไว้มีเฉพาะหน้า login กับไฟล์ที่หน้านั้นต้องใช้ (`OPEN_PATHS` / `OPEN_PREFIXES` ใน `server.js`)

- ยังไม่ล็อกอิน + ขอหน้าเว็บ → `302` ไป `/login.html?next=…`
- ยังไม่ล็อกอิน + ขอ API → `401 {code:"AUTH_REQUIRED"}` (front-end พาไปหน้า login ให้)

### กลไก

| เรื่อง | วิธีทำ |
|---|---|
| รหัสผ่าน | `scrypt` + salt เฉพาะคน เก็บแต่ hash ไม่เคยเก็บรหัสจริง |
| session | คุกกี้เซ็นด้วย HMAC-SHA256 → รีสตาร์ทแล้วยังล็อกอินอยู่ |
| คุกกี้ | `HttpOnly` + `SameSite=Lax` + `Secure` เมื่อมาแบบ https |
| เดารหัส | ผิด 5 ครั้ง (ต่อ IP+ชื่อผู้ใช้) → ล็อก 15 นาที |
| ชื่อผู้ใช้ไม่มีจริง | ยังคำนวณ scrypt หลอกหนึ่งรอบ กันจับเวลาเดาว่าชื่อไหนมีอยู่ |
| ลบผู้ใช้ | session ที่ค้างอยู่ใช้ไม่ได้ทันที (ตรวจกับรายชื่อจริงทุกครั้ง) |
| เพิ่ม/ลบผู้ใช้ | มีผลภายใน 5 วินาที ไม่ต้องรีสตาร์ท (`refreshAuthIfChanged`) |

### role

| role | ดูรายงาน | ใช้ผู้ช่วย AI |
|---|---|---|
| `exec` | ได้ | ได้ (จำกัดโควตาต่อวัน) |
| `viewer` | ได้ | **ไม่ได้** — ทุกคำถามมีค่าใช้จ่ายจริง |

### โหมดทดสอบ `DEV_LOGIN=1`

ไว้เข้าไปดูระบบตอนยังไม่อยากสร้างบัญชีจริง — พิมพ์ชื่อผู้ใช้กับรหัสอะไรก็เข้าได้
ได้ role `exec` เพื่อให้ลองผู้ช่วย AI ได้ด้วย (ถ้าชื่อที่พิมพ์ตรงกับผู้ใช้จริงที่มีอยู่
จะใช้ role กับโควตาของคนนั้นแทน จึงทดสอบความต่าง exec/viewer ได้)

โหมดนี้เท่ากับ **ไม่มีระบบล็อกอิน** จึงล็อกไว้สามชั้น:

1. **เซิร์ฟเวอร์ไม่ยอมสตาร์ท** ถ้า `DEV_LOGIN=1` แต่ `HOST` ไม่ใช่ localhost
   (ป้องกันเผลอเปิดค้างไว้ตอน deploy — ไม่ใช่แค่เตือนแล้วรันต่อ)
2. กรอบเตือนตัวใหญ่ใน log ทุกครั้งที่เปิดเซิร์ฟเวอร์
3. แถบเตือนบนหน้า login และชิปสีเหลืองบนหัว Dashboard (`devLogin` จาก `/api/auth/status`)

ทางที่กั้นอยู่ยังทำงานเหมือนเดิมทุกอย่าง — ยังไม่ล็อกอินก็ยังโดน 302 / 401
ต่างกันแค่ขั้นตอนตรวจรหัสผ่านเท่านั้น

### กันพลาดตอน deploy

เซิร์ฟเวอร์ **ปฏิเสธที่จะเปิด** ถ้า `HOST` ไม่ใช่ localhost แต่ยังไม่มีผู้ใช้เลย
เพราะนั่นคือการเปิดยอดขาย ต้นทุน และรายชื่อลูกค้าให้ทั้งเครือข่ายโดยไม่มีอะไรกั้น
(ข้ามได้ด้วย `ALLOW_NO_AUTH=1` แต่ไม่ควรทำ)

**ยังไม่ได้ทำในโปรเจกต์นี้ ต้องเสริมตอน deploy จริง:** HTTPS (ใช้ reverse proxy เช่น Caddy),
ตัวคุมให้รันต่อเนื่อง (systemd/pm2), และการสำรอง `config/users.json`

### Runbook: มีคนเพิ่มลิงก์ชีตใหม่ในไฟล์ .txt

Dashboard รับ **แท็บใหม่** ในชีตเดิมได้เองทุกครั้งที่รีเฟรช แต่ **ลิงก์ชีตใหม่รับเองไม่ได้**
เพราะ `config/sources.json` เป็นไฟล์ generated ที่ `sync-sources.js` เท่านั้นที่เขียน `sheetId`/`parser` ได้

ระหว่างที่ยังไม่ sync ผู้ใช้จะเห็นแค่แถบ "config ล้าสมัย" (`meta.configOutdated`)
ส่วนลิงก์ใหม่จะไม่ถูกโหลดและ**ไม่มีชื่อไปโผล่ที่ไหนเลย** เพราะ `unmatchedLabels` มาจาก config
ซึ่งยังไม่ถูกเขียนใหม่

```bash
ssh <เซิร์ฟเวอร์>
cd /path/to/kambis
node scripts/sync-sources.js     # อ่าน .txt → เขียน config/sources.json ใหม่
node --test tests/*.js           # ต้องผ่านครบก่อน
systemctl restart kambis         # หรือ pm2 restart
```

ถ้าเป็น **รายงานชนิดใหม่** (ไม่ใช่แค่ชีตเพิ่มของชนิดเดิม) ต้องทำก่อน sync:

1. เพิ่ม entry ใน `PROFILES` ของ `scripts/sync-sources.js` (จับจากคำในชื่อรายงาน)
2. เขียน parser ใน `server/lib/parsers/` แล้วลงทะเบียนใน `parsers/index.js`
3. เพิ่มกฎตรวจใน `analysis.js` ถ้าโครงข้อมูลไม่เหมือน record มาตรฐาน
4. เพิ่ม assertion ใน `tests/smoke.js`

> ⚠ `PROFILES` จับแบบ "มีคำนี้อยู่ในชื่อ" และ**ไม่ตรวจ key ซ้ำ** ถ้าลิงก์ใหม่มีคำว่า
> `สินค้า`/`คงเหลือ`/`ขายดอก` จะถูกจับเป็น key เดิม แล้ว `loader.js` จะเขียนทับกัน
> รายงานตัวแรกหายเงียบ — ตรวจ `config/sources.json` หลัง sync ทุกครั้งว่า key ไม่ซ้ำ

### ค่าที่ reverse proxy ต้องตั้ง

- `proxy_read_timeout` ต้องยาวกว่าเวลาโหลดเต็มรอบ — ตอน Google ล่มรอบหนึ่งใช้เวลาถึง ~3 นาที
  (วัดจริงแล้ว) ค่า default ของ nginx คือ 60 วินาที ซึ่งจะตัดสายกลางคัน
- **`proxy_buffering off` สำหรับ `/api/progress`** ไม่งั้นหน้าจอโหลดจะค้างที่ 0%
  (โค้ดส่ง `X-Accel-Buffering: no` ไว้แล้ว แต่ใช้ได้กับ nginx เท่านั้น)
- `keepAliveTimeout` ของ Node ยังเป็นค่า default 5 วินาที ซึ่งสั้นกว่า upstream keepalive
  ของ proxy ทั่วไป (60 วิ) — อาจได้ 502 เป็นระยะ **ยังไม่ได้แก้**

---

## 9. ธีมและข้อจำกัด Front-end

palette: <https://colorhunt.co/palette/fbf5dde7e1b1306d290d530e>

| Token | Hex | ใช้ทำอะไร |
|---|---|---|
| `--paper` | `#FBF5DD` | ครีมสว่าง — พื้นหลังโหมดสว่าง / ตัวอักษรโหมดมืด |
| `--khaki` | `#E7E1B1` | ครีมเข้ม — สีเน้นในโหมดมืด |
| `--leaf` | `#306D29` | เขียว — สีแบรนด์ |
| `--forest` | `#0D530E` | เขียวเข้ม — สีเน้นในโหมดสว่าง / พื้นหลังโหมดมืด |

### โหมดสว่าง/มืด

Dashboard รองรับทั้งสองโหมด สลับด้วยปุ่มบน header

- ค่าเริ่มต้นเดินตาม `prefers-color-scheme` ของระบบ
- เมื่อผู้ใช้กดเลือกเอง ค่าจะถูกจำไว้ใน `localStorage` (`kambis.theme`) และชนะค่าจากระบบ
- `<html data-theme="light|dark">` ถูกตั้งใน `<head>` ก่อน paint เพื่อไม่ให้จอกระพริบตอนโหลด
- ตรรกะทั้งหมดอยู่ใน `public/js/theme.js` ค่าสีทั้งหมดอยู่ใน `public/css/tokens.css`

**กฎการเขียนสี:** ห้ามฮาร์ดโค้ดค่าสีใน CSS หรือ JS นอก `tokens.css` เด็ดขาด
ทุกที่ต้องอ้างผ่าน custom property ไม่งั้นโหมดใดโหมดหนึ่งจะพัง
ลำดับของกฎใน `tokens.css` มีความหมาย (`:root` → media query → `[data-theme]`) ห้ามสลับ

### ขนาดตัวอักษร — ใช้โทเคนเท่านั้น

**กฎเดียวกับสี: ห้ามใส่ `font-size` เป็น px/rem ลอย ๆ** ทุกที่ต้องอ้าง `--fs-*` ใน `tokens.css`
(`--fs-4xs` … `--fs-3xl` บวก `--fs-kpi` / `--fs-metric` สำหรับตัวเลขพาดหัว)

ทั้งชุดคูณด้วย `--fs-scale` ปุ่มเดียว ตอนนี้ตั้งไว้ **`1.17`** บนจอใหญ่ และ `1.1` บนจอ < 768px
(ใน `responsive.css`) — ลำดับตัวอักษรจึงเหมือนกันทุกจอ ต่างแค่ทั้งชุดเล็กลง
เดิมมี `font-size` กระจาย 112 จุดใน 9 ไฟล์ด้วยค่า 20 แบบ การ "ขยายตัวอักษรทั้งเว็บ"
จึงเป็นงานที่ลืมบางจุดเสมอ แล้วลำดับหัวข้อ-เนื้อความจะเพี้ยนทีละนิดโดยไม่มีใครสังเกต

**ฟอนต์บน canvas เดินตามสเกลนี้ด้วย** ผ่าน `chartFont()` ใน `charts/core.js`
(อ่าน `--fs-*` ตรง ๆ ไม่ได้เพราะเป็น `calc()` แต่ `--fs-scale` เป็นตัวเลขล้วนจึงคูณเองได้)

**สิ่งที่ต้องตรวจทุกครั้งที่ขยับ `--fs-scale`** — สามอย่างนี้พังก่อนเพื่อนเสมอ:

1. ความสูงตายตัวเป็น px (`height: 32px` ของ pill/ช่องกรอก) จะบีบตัวอักษรจนล้นกรอบ
   → ใช้ `min-height` เสมอ
2. คอลัมน์กว้างตายตัว (`grid-template-columns: 84px …`) จะตัดข้อความทิ้ง
   → ใช้ `minmax(84px, max-content)`
3. ป้ายบนกราฟที่ยาวเกินช่องของแท่งจะถูก `drawXLabels` ข้ามทิ้งเงียบ ๆ
   → `stackedBars` ตัดป้ายให้พอดี `slot - 12` ก่อนวาด (ต้องมากกว่า `minGap` จริง ๆ
   เพราะเงื่อนไขการชนใช้ `<` ไม่ใช่ `<=` — พอดีเป๊ะแปลว่าชน)

### ระยะห่าง: ระหว่างกลุ่มต้องมากกว่าในกลุ่มเสมอ

| ที่ | ระยะ |
|---|---|
| ระหว่างแผง (`.page` · `.page-grid` · `.stack`) | `--sp-6` 32px |
| ระหว่างการ์ด (`.card-grid`) | `--sp-5` 24px |
| ระหว่างช่องตัวเลข (`.kpi-strip`) | `--sp-4` 16px |
| ในแผง / ในการ์ด | `--sp-5` 24px · `--sp-4` 16px |

เดิมทั้ง `.page` และ `.panel` ใช้ `--sp-4` เท่ากันเป๊ะ แผงจึงอ่านเป็นก้อนเดียว
และหน้า Supply หนักกว่านั้น: แผงอยู่ใน `div` เปล่าที่หน้าวาดใหม่เอง จึงเป็น **หลาน** ของ `.page`
ไม่ได้ `gap` เลย (วัดได้ 0px) — กล่องแบบนั้นต้องใส่คลาส **`.stack`** เสมอ

### ตัวเลขพาดหัวต้องอยู่ในกรอบเสมอ

`.kpi__value` และ `.card__metric-value` **ไม่ได้ตั้งขนาดตายตัว** — คิดจาก
ความกว้างจริงของการ์ดหารด้วยจำนวนอักขระ แล้ว clamp ด้วยเพดาน `--fs-kpi` / `--fs-metric`

```css
font-size: clamp(var(--fs-lg), calc(100cqi / var(--val-len) * 1.62), var(--fs-kpi));
```

- `--val-len` มาจาก `valueLen()` ใน `format.js` ที่ผู้สร้างการ์ดใส่ไว้ใน `style="--val-len:N"`
  (`renderKpiStrip` และ `cardShell` ใน `ui/cards.js` · `tiles()` ใน `pages/shared.js`)
  **ผู้สร้างการ์ดใหม่ต้องส่งค่านี้ด้วย** ไม่งั้นจะใช้ค่าเริ่มต้น 8 อักขระ แล้วเลขยาวกว่านั้นจะล้น
- การ์ดต้องประกาศ `container-type: inline-size` ให้หน่วย `cqi` มีที่อ้าง
- ยอดเงินระดับบริษัทเป็นเลข 8 หลักบวกหน่วย (`17,299,482฿`) ซึ่งกว้างเกินช่อง 11rem แบบเดิม
  `.kpi-strip` จึงใช้ `minmax(16rem, 1fr)` — **ความกว้างขั้นต่ำแก้ที่เดียวตรงนี้**
  อย่าไปลดขนาดตัวอักษรทีละที่เพื่อให้พอดี

### ตัวเลขเงินที่ติดลบ

ขาดทุนต้องเห็นโดยไม่ต้องไล่หาเครื่องหมายลบในเลขหลักล้าน แต่ **สีแดงอย่างเดียวไม่พอ**
ทุกที่ที่ย้อมสีต้องมีคำว่า "ขาดทุน" หรือเครื่องหมายลบกำกับด้วยเสมอ เพราะใต้ตาบอดสีแดง-เขียว
ผู้ใช้เห็นว่า "มีการทำเครื่องหมายไว้" แต่ไม่รู้ว่าแปลว่าอะไร และแคปหน้าจอขาวดำก็หายไปเลย

| เรื่อง | กฎ |
|---|---|
| โทเคน | `--money-neg` / `--money-pos` **ห้ามยืม `--sev-*`** |
| ช่องไหนย้อม | ผู้เรียกประกาศ `tone: 'signed'` เอง **ห้ามให้ `tiles()` เดาจากค่าติดลบ** |
| คำกำกับ | `lossHint()` ใน `pages/shared.js` เติมคำว่า "ขาดทุน" ให้ |
| ในตาราง | `signedBaht()` ใน `pages/cost.js` — ต้องใส่**ทุกคอลัมน์ที่ติดลบได้** |

- **แยกโทเคนจาก severity เพราะสองความหมายอยู่บนหน้าเดียวกัน** — แดงแบบ finding แปลว่า
  "ตัวเลขนี้เชื่อไม่ได้ ไปแก้ที่ชีต" ส่วนแดงแบบขาดทุนแปลว่า "ตัวเลขถูก แต่ผลติดลบ"
  หน้าต้นทุนมีทั้งคู่ (critical 13 ข้อ) ต้องขยับสีทีละฝั่งได้
- **โหมดมืดใช้แดงคนละเฉดกับ `--sev-critical`** คู่ `#ff8a80` / `#86d992` ต่างกันแค่ ΔE 8.1
  ใต้ตาบอดสีเขียว (เกณฑ์ 11) จึงดึง L\* ลง 16 หน่วยเป็น `#ec7a71` → ΔE 12.8 · contrast 4.74:1
- **ห้ามย้อมตาม "เงินเข้า/เงินออก"** ค่าเสื่อมราคาและต้นทุนเก็บเป็นเลขบวกทั้งที่เป็นรายจ่าย
  ถ้าย้อมตามความหมายหน้าต้นทุนจะแดงทั้งหน้าจนสีไม่เหลือความหมาย
- **ติดลบไม่ได้แปลว่าแย่เสมอ** — หน้า Supply มีรายการ "เบิกลดลง" ที่ติดลบแล้วเป็นสีเขียว
  (`data-level="good"`) นี่คือเหตุผลที่กฎต้องประกาศต่อช่อง ไม่ใช่เดาจากเครื่องหมาย

### กราฟต้องรองรับค่าติดลบ

`drawYAxis()` วนจาก 0 ขึ้นบนอย่างเดียว ค่าติดลบจึงได้พิกัด **ใต้ผืนผ้าใบ** แล้วหายไปเงียบ ๆ
เคยทำให้เส้นกำไรรายเดือนที่ขาดทุนสี่เดือนแรกดูเหมือน "ไม่มีข้อมูล" ทั้งที่ความจริงคือติดลบ
— อันตรายกว่าการที่มันไม่แดงมาก

กราฟเส้นจึงใช้ `drawYAxisRange(ctx, box, min, max)` ที่คืน `{top, bottom, span}`
และวาด**เส้นศูนย์เข้มกว่าเส้นตารางอื่น** ส่วนพื้นที่ใต้เส้นต้องมีฐานที่ `yAt(0)` ไม่ใช่ก้นกล่อง

### หน่วยของตัวเลขในกราฟ

ทุกกราฟจัดรูปแบบค่าผ่าน `fmtValue(value, unit)` ตัวเดียวใน `charts/index.js`
`'g'` (ค่าเริ่มต้น) = น้ำหนัก ย่อเป็น kg ให้ · `'%'` = เปอร์เซ็นต์ · หน่วยอื่นเช่น `'฿'` = เลขล้วน + หน่วย

**เคสจริง:** `barH()` กับ `donut()` เคยเรียก `weight()` ตายตัวโดยเมิน `unit` ที่ผู้เรียกส่งมา
โดนัทสัดส่วนต้นทุนจึงขึ้นกลางวงว่า **"17,299 kg"** จากยอด 17,299,482 บาท
และแท่ง "วัสดุที่มีมูลค่าสูงสุด" ที่มีป้ายกำกับว่า "หน่วยเป็นบาท" แสดงเป็น kg ทุกแถว
ส่วน `line()` ไม่เคยรับ `unit` เลย tooltip ของกราฟหน้าต้นทุนจึงบอกรายได้เป็น kg

**แกน Y ต้องบอกหน่วยด้วย** — `axisUnit(unit, max)` ใน `charts/core.js` แปลงค่าบนแกน
เป็นหน่วยเดียวกับ tooltip แล้วเขียนหน่วยกำกับที่ **ป้ายบนสุดป้ายเดียว** (`400 kg` · `4.0M ฿` · `100 %`)
เดิมแกนเป็นตัวเลขเปล่า `400k` ซึ่งคือ 400,000 กรัม แต่ไม่มีอะไรบอกว่าเป็นกรัมหรือกิโล
ทุกป้ายไม่ต้องมีหน่วยเพราะช่องแกนกว้างแค่ ~45px — ใส่ทุกป้ายแล้วตัวเลขจะถูกตัด

### สีของกราฟ

สีของกราฟ **ไม่ใช่** สี UI — เป็นชุดที่ derive มาแล้วและผ่านการตรวจ 6 ข้อ
(lightness band, chroma, CVD separation, normal-vision floor, contrast) แยกกันทั้งสองโหมด

| บทบาท | โหมดสว่าง | โหมดมืด |
|---|---|---|
| `--series-1` (ดอก) | `#93801A` | `#A89526` |
| `--series-2` (ไม่ใช่ดอก) | `#04724F` | `#2C8D64` |
| `--size-xxl` … `--size-xs` | 6 สีคนละเนื้อสี เรียงเข้ม→สว่างตามขนาด | ชุดเดียวกัน ยกความสว่างขึ้น |
| `--stage-1` … `--stage-4` | ไล่เฉดเขียวของช่วงการปลูก | ไล่เฉดเขียวโหมดมืด |
| `--cat-1` … `--cat-8` | ชุดสีแยกหมวด 8 ช่อง | ชุดสีแยกหมวด 8 ช่อง |
| พื้นวางกราฟ `--chart-well` | `#FFFDF4` | `#0A2E0A` |

ถ้าจะเปลี่ยนสีกราฟ ต้องรัน validator ใหม่ทั้งสองโหมดก่อนเสมอ (ดู skill `dataviz`)
กราฟวาดลงบน canvas จึงไม่เปลี่ยนสีตาม CSS เอง — `main.js` วาดใหม่ทั้งหมดเมื่อสลับธีม

#### `--size-*` — คนละเนื้อสี แต่ยังเรียงความสว่างตามขนาด

ผู้ใช้สั่งให้ขนาดดอกแต่ละขนาด **ใช้คนละสีไปเลย** ("เลือกสีให้ต่างกัน") ไล่เฉดเขียว 6 ขั้นแบบเดิม
แยกด้วยตาไม่ออกบนแท่งแคบ ๆ และกำหนดรหัสสีมาให้สองช่อง: **XS `#FFD93D` · S `#FF9A00`
สองค่านี้ห้ามขยับ** อีก 4 ช่องเลือกด้วยการค้นทั้งวงสีทีละ 6° ให้ ΔE ต่ำสุดของทุกคู่สูงที่สุด
(สคริปต์ค้นอยู่ในสกิล `dataviz` — ผลลัพธ์เขียนไว้ใน `tokens.css` แล้ว)

| โหมด | XXL | XL | L | M | S | XS |
|---|---|---|---|---|---|---|
| สว่าง | `#00580D` | `#4B52B9` | `#C04775` | `#1F98ED` | `#FF9A00` | `#FFD93D` |
| มืด | `#198223` | `#6670DA` | `#DA5F8B` | `#37A9FE` | `#FF9A00` | `#FFD93D` |

กฎที่ต้องรักษาไว้เมื่อแตะชุดนี้:

- **ความสว่างต้องไล่ลงตามขนาด** (XXL เข้มสุด → XS สว่างสุด) ขนาดดอกเป็นข้อมูลที่มีลำดับ
  ถ้าปล่อยความสว่างสะเปะสะปะจะอ่านลำดับจากกราฟไม่ได้ แม้เนื้อสีจะต่างกันแล้วก็ตาม
- **เนื้อสีของแต่ละขนาดต้องเป็นตัวเดียวกันทั้งสองโหมด** เปลี่ยนได้แค่ความสว่าง
  ไม่งั้นสลับธีมแล้ว XXL เปลี่ยนจากเขียวเป็นสีอื่น คนจำสีไม่ได้
- ขั้นล่างสุดของโหมดมืดถูกจำกัดด้วย contrast 3:1 บนพื้นกราฟเขียวเข้ม จึงลงได้แค่ `#198223`
- ชุดนี้ **ตรวจด้วยโหมด categorical ไม่ใช่ `--ordinal` อีกแล้ว** (ไม่ใช่ไล่เฉดโทนเดียว)
  ผลที่ได้ทั้งสองโหมด: CVD ΔE ต่ำสุด **11.6** · ตาปกติต่ำสุด **14.7** — ทั้งสองค่ามาจาก
  **คู่ S/XS ที่ผู้ใช้ตรึงไว้เอง** (ส้มกับเหลืองอยู่ใกล้กันในวงสี) ทุกคู่ที่เหลือห่างกว่านี้
  ค่า 14.7 ต่ำกว่าเกณฑ์ 15.0 ของ validator นิดเดียว และ contrast ของ S/XS บนพื้นครีม
  ได้แค่ 2.09:1 / 1.35:1 — **เป็นการแลกที่ผู้ใช้เลือกเอง ไม่ใช่ของหลุด** เหมือนกรณี `--bud-*`
- validator จะขึ้น FAIL ข้อ "lightness band" ด้วย เพราะชุดนี้จงใจกินช่วงความสว่างกว้าง
  เพื่อรักษาลำดับขนาด — เกณฑ์ข้อนั้นตั้งไว้สำหรับชุดหมวดที่ความสว่างควรใกล้กัน

**`--stage-*` — ไล่เฉดเขียวของช่วงการปลูก** กราฟไทม์ไลน์ครอป (Veg → Flower → Harvest → Dry)
เคยยืมขั้นของ `--size-*` มาใช้ พอสีขนาดดอกเลิกเป็นไล่เฉดโทนเดียว กราฟนี้จะกลายเป็นสีสลับไปมา
ทั้งที่เป็นลำดับเวลา จึงแยกเป็นโทเคนของตัวเอง = ไล่เฉดเขียวชุดเดิมที่เคยใช้อยู่

**`--size-*` กับ `--cat-*` ใช้แทนกันไม่ได้**

- `--size-*` มี **ลำดับ** (ขนาดดอกใหญ่→เล็ก) จึงล็อกความสว่างไว้ตามลำดับนั้น
  และ tooltip ต้องคงลำดับเดิม ไม่เรียงตามน้ำหนัก
- `--cat-*` เป็นหมวดที่ **ไม่มีลำดับ** (สายพันธุ์ / คลัง) ตรวจด้วยโหมด categorical
  ไล่ lightness สลับสูง-ต่ำระหว่างช่องที่ติดกันโดยตั้งใจ เพราะภายใต้ตาบอดสีแดง-เขียว
  ข้อมูลเนื้อสีเกือบหายหมด ระยะห่างจึงต้องมาจากความสว่าง
  (ผ่านแล้วทั้งสองโหมด: CVD ΔE ≥ 11 · normal ≥ 16.6 บนพื้นของ Kambis)

เลือกชุดสีผ่าน `opts.ramp` ของ `stackedBars()` / `donut()` — **ห้ามส่ง hex จากผู้เรียก**
เพราะ palette ต้องอ่านจาก tokens.css ตอนวาดทุกครั้ง ไม่งั้นสลับธีมแล้วสีไม่เปลี่ยนตาม

**`ramp: 'size3'` — 3 สีสำหรับกราฟที่ยุบขนาดดอกเหลือ 3 กลุ่ม** อยู่ที่ `--bud-*`
(น้ำตาล `#4F200D` · ส้ม `#FF9A00` · เหลือง `#FFD93D`) ผู้ใช้กำหนดรหัสมาเองทั้งสามสี
**ไม่ได้หยิบมาจาก `--size-*`** — S กับ XS ใช้รหัสเดียวกันกับชุด 6 ขนาดอยู่แล้ว
ส่วน "ดอกใหญ่" เป็นน้ำตาล ไม่ใช่เขียวแบบ `--size-xxl` (คนละชุดกัน ตั้งใจให้ต่างได้)

**8 ช่อง เรียงตายตัว ห้ามวน** — เกิน 8 หมวดให้ยุบเป็น "อื่น ๆ" สี `--ink-mute`
และต้องสร้างตัวจับหมวด→สี (`topCategories()`) จาก **ข้อมูลทั้งชุดครั้งเดียว**
ห้ามสร้างใหม่จากข้อมูลที่กรองแล้ว ไม่งั้นเปลี่ยนตัวกรองทีสีจะสลับกันทั้งกราฟ
(สีต้องผูกกับตัวสายพันธุ์ ไม่ใช่กับอันดับของมันในชุดที่กำลังดู)

> หมายเหตุที่ค้างไว้: คู่ `S`/`XS` (ส้ม/เหลืองที่ผู้ใช้กำหนด) ได้ ΔE ตาปกติ 14.7
> ต่ำกว่าเกณฑ์ 15.0 ของ validator อยู่นิดเดียว ขยับให้ผ่านได้ด้วยการดัน XS
> ไปทางเหลืองอ่อนอีกเล็กน้อย แต่ยังไม่ทำเพราะเป็นรหัสสีที่ผู้ใช้สั่งมาตรง ๆ

### ตัวกรองและการจัดเรียง

ทุกการ์ดและทุกแผงในหน้ารายละเอียดมีแถบควบคุมชุดเดียวกัน (`public/js/ui/controls.js`)

| ตัวเลือก | ทำอะไร |
|---|---|
| มุมมอง | สลับมิติของข้อมูล เช่น ตามสายพันธุ์ / ตามครอป / แนวโน้มตามเวลา |
| จัดเรียง | มาก→น้อย, น้อย→มาก, เก่า→ใหม่, ใหม่→เก่า, ตามชื่อ |
| กรอง | แสดงทั้งหมด / เฉพาะที่มีข้อมูล / 5 อันดับแรก |

กฎที่ต้องรักษาไว้:

- ตัวเลือก "เก่า→ใหม่ / ใหม่→เก่า" จะโผล่**เฉพาะเมื่อ key เป็นช่วงเวลาจริง** และเป็นค่าเริ่มต้น
  ของข้อมูลประเภทนั้น — ข้อมูลตามเวลาต้องเรียงตามเวลา ไม่ใช่ตามขนาด
- ค่า `null` ถูกดันไปท้ายเสมอไม่ว่าเรียงทางไหน (`—` แปลว่าไม่มีข้อมูล ไม่ใช่ 0)
- `<select>` บนการ์ดต้อง `stopPropagation()` ทุก event เพราะทั้งการ์ดเป็นปุ่มเปิด modal
- **ห้ามตั้งชื่อ property ว่า `valueOf`** ทุก object มี `Object.prototype.valueOf` ติดตัวอยู่แล้ว
  อ่านค่าออกมาจะได้เมธอดของ Object แทน `undefined` ทำให้ค่า default ไม่ทำงานแล้วพัง
  (ใช้ชื่อ `getValue` แทน — เคยพังมาแล้วจริง)

ในหน้ารายละเอียด ตารางข้อมูลดิบกรองได้ 4 ทางพร้อมกัน: มิติหลัก · สายพันธุ์ · ช่วงวันที่ · คำค้น
และเรียงได้ทุกคอลัมน์โดยกดที่หัวตาราง

### การกำหนดขนาด canvas (เคยพังมาแล้ว 2 อาการ)

กราฟวาดลง canvas ซึ่งมี "ขนาดที่วาด" (`canvas.width`) แยกจาก "ขนาดที่แสดง" (CSS)
ถ้าสองค่านี้ไม่ตรงกัน เบราว์เซอร์จะยืดหรือบีบภาพให้พอดี — ผลคือ
**วงกลมกลายเป็นวงรี และป้ายชื่อเลื่อนไปทับแท่ง**

กฎสองข้อใน `setupCanvas()`:

1. **วัดจาก content box ของกล่องแม่ ไม่ใช่ `clientWidth` เฉย ๆ**
   `clientWidth` นับ padding รวมด้วย แต่ canvas ที่ `width:100%` กว้างเท่า content box
   (`.chart-well` มี padding 12px สองข้าง เพี้ยนไป 24px = บีบ 7%)
2. **วัดไม่ได้ให้คืน `null` ห้ามเดาเป็น 300**
   ค่าเดาทำให้ทุกการ์ดถูกยืด/บีบด้วยอัตราส่วนที่ต่างกันไป

ข้อ 2 บังคับให้ **ต้องวาดกราฟหลังจากอิลิเมนต์เข้า DOM แล้วเท่านั้น**

- `cards.js` เก็บงานวาดไว้ใน `drawLater[]` แล้วรันหลัง `el.appendChild(card)` ครบทุกใบ
- `modal.js` เปิด modal (`is-open`) **ก่อน** เรียก `buildBody()` เพราะตอนซ่อนอยู่กว้าง 0

### ป้ายแกน X ของกราฟ

ใช้ `drawXLabels()` จาก `charts/core.js` เท่านั้น — มัน `measureText` แล้วข้ามป้ายที่จะชนกัน
**ห้ามกลับไปใช้วิธีเว้นระยะตามจำนวนข้อมูล (stride) แล้วบังคับวาดป้ายสุดท้ายเสมอ**
เพราะป้ายสุดท้ายจะไปทับป้ายที่ stride เลือกไว้เมื่อระยะบังเอิญใกล้กัน (เคยเกิดกับกราฟเทียบขนออก↔รับเข้า)

**กราฟแท่งต้องส่ง `{ center: true, bounds: [2, w - 2] }` เสมอ** (`stackedBars` · `groupedBars`)

ค่าเริ่มต้นของ `drawXLabels()` ดันป้ายแรกไปชิดซ้ายและป้ายสุดท้ายไปชิดขวาของกรอบ
ซึ่งถูกสำหรับ **กราฟเส้น** เพราะจุดแรก/สุดท้ายอยู่ที่ขอบกรอบพอดี วางกึ่งกลางแล้วจะล้น canvas
แต่กับ **กราฟแท่ง** จุดแรกอยู่ลึกเข้ามาครึ่งช่อง การดันไปชิดขอบทำให้ป้ายยาว ๆ
กินพื้นที่ของแท่งข้าง ๆ แล้ว **ป้ายของแท่งที่ 2 กับรองสุดท้ายหายไปทั้งคู่**
(เจอจริง: ชื่อครอปหายสองแท่งทั้งที่ที่ว่างพอ ส่วนบรรทัดวันที่ซึ่งสั้นกว่ายังอยู่ครบ
จึงดูเหมือนข้อมูลบางแท่งขาด ทั้งที่เป็นเรื่องการจัดวางป้ายล้วน ๆ)

### ป้ายใต้แกน x ต้องเป็นของสิ่งที่แท่งนั้นรวมมาจริง ๆ

กราฟ "ผลผลิตตามช่วงเวลา แยกตามสายพันธุ์" เคยเขียนชื่อครอปไว้ใต้ชื่อเดือน โดยเลือก
**ครอปที่ให้ผลผลิตมากที่สุดของเดือนนั้น** มาเขียน — แท่งเป็นยอดรวมของหลายครอป
แต่ป้ายบอกชื่อครอปเดียว คนอ่านจึงเข้าใจว่าทั้งเดือนมาจากครอปนั้น

ครอปย้ายไปอยู่ใน tooltip แทน ผ่าน `row.detail[หมวด] = [{label, value}]` ของ `stackedBars()`
(เรียงมากไปน้อย · ยอดต้องบวกได้เท่าหมวดแม่) แล้วกราฟเลือกวิธีแสดงเอง:

| ที่มาของหมวดนั้น | แสดงเป็น |
|---|---|
| ครอปเดียว | ต่อท้ายบรรทัดเดิม — `Royal Gorilla: 21.4 kg (16.6%) · G1/1-17NOV25` |
| หลายครอป | แตกเป็นบรรทัดย่อยเยื้องเข้า ครอปละน้ำหนักและ % ของตัวเอง |

- **% ของบรรทัดย่อยคิดจากยอดรวมทั้งแท่ง เหมือนบรรทัดแม่** บวกกันแล้วต้องได้ % ของแม่พอดี
  (ถ้าคิดจากยอดของสายพันธุ์จะได้ 100% ทุกกลุ่ม แล้วเทียบข้ามสายพันธุ์ไม่ได้)
- **ลิสต์ครบทุกครอป ห้ามยุบเป็น "อีก N ครอป"** — ผู้ใช้สั่งไว้ชัดว่าเดือนไหนสายพันธุ์นั้น
  มีกี่ครอปให้ขึ้นให้หมด ยอมให้กล่องยาวขึ้นดีกว่าต้องไปเปิดตารางข้างล่างหาที่เหลือ
  `.chart-tip` จึงใช้ `line-height: 1.38` (แน่นกว่าเนื้อความทั่วไป) ให้เคสยาวสุดที่มีจริง
  (เม.ย. — 4 สายพันธุ์ 9 ครอป = 14 บรรทัด ≈ 267px) ยังอยู่ในกรอบกราฟสูง 260px
- **แถวที่ไม่รู้ครอปต้องโผล่เป็น `ไม่ระบุครอป`** ห้ามเดาจากครอปอื่นของเดือนนั้น
  และห้ามปล่อยหาย ไม่งั้นบรรทัดย่อยจะบวกไม่ถึงยอดของสายพันธุ์โดยไม่มีอะไรอธิบาย
- `.chart-tip` กว้างได้ไม่เกิน 26rem และ `attachTooltip()` ดันกล่องกลับเข้ากรอบ**ทั้งแนวตั้งและแนวนอน**
  ไม่งั้นกล่องที่มีบรรทัดย่อยจะไหลลงไปทับกราฟใบถัดไปบนจอเล็ก
- **ลำดับบรรทัดใน tooltip ตามชนิดของหมวด** — หมวดที่ไม่มีลำดับ (สายพันธุ์) เรียงมากไปน้อย
  เพื่ออ่านเป็นการจัดอันดับ ส่วนหมวดที่มีลำดับ (`ramp: 'size'` / `'size3'`) ต้องคงลำดับเดิม
  ให้ตรงกับชั้นในแท่งและ legend ไม่งั้นจะได้ `XS` มาก่อน `S` ในครอปที่ XS เยอะกว่า

### กราฟ "สัดส่วนขนาดดอกต่อครอป" ยุบ 6 ขนาดเหลือ 3 กลุ่ม

`ดอกใหญ่ (≥M)` · `S` · `XS` — คำถามที่กราฟนี้ตอบคือ "สัดส่วนดอกใหญ่ขยับไปทางไหน"
ซึ่ง 6 เฉดของไล่สีเดียวกันบนแท่งกว้าง 38px แยกด้วยตาไม่ออกอยู่ดี

**ข้อมูลไม่ได้หายไป** — XXL/XL/L/M ทีละขนาดพร้อม % อยู่ครบใน tooltip ผ่าน `detail`
เรียงจากใหญ่ไปเล็กตามลำดับขนาดจริง ไม่ใช่ตามน้ำหนัก (เป็นข้อมูลที่มีลำดับ)
ส่วนกราฟอื่นที่ยังใช้ 6 ขนาด (`sizeMixBar` บนการ์ดภาพรวม · หน้าสต็อก) ไม่ถูกแตะ

### ข้อจำกัดอื่น

- **Vanilla JS เท่านั้น** ห้าม framework ใด ๆ ยกเว้น Three.js (vendor ไว้ใน `public/vendor/`)
- **Zero npm dependencies** ทั้งฝั่ง server และ client
- Glassmorphism ลึกได้ 3 ชั้น: พื้นหลัง Three.js → การ์ด → modal
- Responsive 375 → 1920 ห้ามมี horizontal scroll ระดับหน้า
- เคารพ `prefers-reduced-motion: reduce` — ปิด Three.js ใช้ gradient นิ่ง
- ภาษา: ไทยเป็นค่าเริ่มต้น สลับ EN ได้ ทุกข้อความ chrome ต้องมีคีย์ครบสองภาษาใน `public/js/i18n.js`

---

## 10. การแสดง finding

ทุก finding บนการ์ด "คุณภาพข้อมูล" ต้องบอกให้ครบว่าปัญหาอยู่ตรงไหนของต้นทาง:

> 📄 **ชื่อไฟล์ Google Sheet › ชื่อ tab** (กดแล้วเปิดตรงไปที่ tab นั้น) · แถวที่ · ช่อง

- ชื่อไฟล์มาจาก `meta.sources[].titleTh/titleEn`, ลิงก์จาก `meta.sources[].sheetUrl`
  ทั้งคู่สืบมาจาก `config/sources.json` → `แบบฟอร์มรายงาน Kambis.txt`
- ลิงก์ต้องพาไปที่ tab โดยตรง: `{sheetUrl}?gid={gid}#gid={gid}`
  (Google Sheets ต้องมี gid ทั้งใน query และ fragment ถึงจะเด้งไป tab นั้นจริง)
- **finding ที่เกิดจากการเทียบข้ามรายงาน ต้องแนบลิงก์ครบทุกชีตที่เกี่ยวข้อง**
  ผู้ใช้ต้องเปิดดูทั้งสองฝั่งถึงจะตัดสินได้ว่าฝั่งไหนผิด — ให้ลิงก์เดียวไม่พอ

ห้ามแสดง finding โดยไม่บอกไฟล์ต้นทาง เพราะผู้ใช้จะตามไปแก้ไม่ได้

### การ์ดคุณภาพข้อมูล — ทั้งสองรายงาน และอยู่ท้ายหน้าเสมอ

| รายงาน | หน้า | payload ที่ใช้ |
|---|---|---|
| Dryflower | `#/dryflower/overview` | `/api/reports` |
| Supply | `#/supply` | `/api/reports/supplyLog` (คนละก้อน โหลดแยก) |

- การ์ดนี้ **ไม่ได้อยู่ในชุดของ `renderCards()`** แต่หน้าเป็นคนวางเองด้วย
  `appendQualityCard()` จาก `pages/shared.js` เพราะผู้ใช้กำหนดว่าต้องเป็น
  **การ์ดใบสุดท้ายของหน้า** ซึ่งอยู่หลังการ์ด "รอข้อมูล" ที่หน้าเป็นคนสร้าง
- **ต้องส่ง payload ของรายงานนั้นเข้าไปเสมอ** ชีตวัสดุโหลดแบบ lazy จึงมี `meta`/`analysis`
  คนละก้อนกับ Dashboard หลัก ถ้าเผลอใช้ก้อนของ Dryflower หน้า Supply จะโชว์
  finding ของรายงานดอก (`onOpen(key, trigger, override)` มีพารามิเตอร์ที่สามไว้เพื่อการนี้)

### หมายเหตุ "ข้อมูลที่ยังขาด" — บังคับมีทั้งสองรายงาน

`public/js/ui/gaps.js` เป็นทะเบียนกลางว่าแต่ละรายงานยังขาดอะไรถึงจะทำ Dashboard ได้ครบ
แสดงสองที่: มุมมองหนึ่งบนการ์ดคุณภาพข้อมูล และแผงเต็มใน modal ของการ์ดนั้น

**ทำไมต้องมีแยกจาก finding:** คะแนน 100/100 แปลว่า "ตัวเลขที่มีไม่ขัดกันเอง"
ไม่ได้แปลว่า "มีตัวเลขครบ" — ผู้ใช้ต้องเห็นทั้งสองอย่างถึงจะตัดสินใจถูก

- ทุกข้อต้องบอกได้ว่า **ต้องเพิ่มคอลัมน์อะไร ที่ชีตไหน** (กฎข้อ 10 ใช้กับที่นี่ด้วย) — มี test คุมไว้
- **ข้อที่ข้อมูลมาครบแล้วต้องหายไปเอง** ผ่าน `detailOf()` (คืน `false` = ไม่ใช่ปัญหาแล้ว)
  เช่นถ้าวันหนึ่งของที่ต้องสั่งมีราคาครบทุกรายการ ข้อ "ราคาไม่ครบ" ต้องไม่ค้างอยู่ให้คนเข้าใจผิด
- การ์ด "รอข้อมูล" (`ui/placeholder.js`) ตามหน้าต่าง ๆ ยังอยู่เหมือนเดิม ทำคนละหน้าที่กัน:
  การ์ดนั้นอยู่ตรงจุดที่ควรมีตัวเลขแต่ไม่มี ส่วนทะเบียนนี้คือสรุปรวมของทั้งรายงาน


---

## 11. Chatbot วิเคราะห์ข้อมูล

ช่องแชทมุมขวาล่างให้ถามคำถามเกี่ยวกับข้อมูลใน Dashboard โดยเรียก **Gemini ผ่าน Google AI Studio**

### ส่วนไหนใช้ AI และส่วนไหนไม่ใช้

| ส่วน | ใช้ AI ไหม |
|---|---|
| ดึงข้อมูลจาก Google Sheets / กดรีเฟรช | **ไม่ใช้** — CSV parsing ด้วย JavaScript ล้วน ใช้ 0 token |
| ค้นหาแท็บใหม่ | **ไม่ใช้** — อ่าน HTML ของหน้า htmlview ด้วย regex |
| Data Analysis ตรวจความถูกต้อง | **ไม่ใช้** — เป็นกฎที่เขียนไว้ใน `analysis.js` |
| Chatbot ตอบคำถาม | **ใช้** — เรียก Gemini ทุกครั้งที่ถาม |

**อย่าทำช่องแสดง token ของการรีเฟรช** เพราะจะขึ้น 0 ตลอดและทำให้เข้าใจผิด
ตัวเลข usage ที่แสดงบน UI เป็นของ Chatbot เท่านั้น

### สถาปัตยกรรม

```
เบราว์เซอร์ ──POST /api/chat──▶ server/server.js
                                      │  ตรวจ role + โควตา แล้วประกอบ system
                                      ▼
                            server/lib/chat-context.js   สรุป payload เป็นข้อความ
                                      ▼
                            server/lib/gemini.js         เรียก generativelanguage.googleapis.com
```

- **API key อยู่ฝั่ง server เท่านั้น** อ่านจาก `GOOGLE_API_KEY` (หรือ `GEMINI_API_KEY`)
  ห้ามส่ง key ไปฝั่งเบราว์เซอร์เด็ดขาด ใครเปิดหน้าเว็บก็อ่านได้
- ไม่ใช้ SDK ของ Google เพราะโปรเจกต์ตั้งกฎ zero dependencies ไว้
  เรียก REST ตรงด้วย `node:https` แลกกับที่ต้องจัดการ retry/timeout เอง

### โมเดล

ตารางอยู่ที่ `server/lib/gemini.js` → `MODELS` ทุกตัว**ทดสอบกับ key จริงแล้วว่าเรียกได้**

| id | ลักษณะ |
|---|---|
| `gemini-3.5-flash-lite` | เร็วที่สุด ~1 วิ ไม่มีขั้นตอนคิด |
| `gemini-3.5-flash` | ค่าเริ่มต้น สมดุลความเร็วกับความละเอียด |
| `gemini-3.6-flash` | ละเอียดที่สุด เหมาะกับคำถามที่ต้องเทียบหลายตัวเลข |

**ไม่ใส่รุ่น Pro ในรายการ** เพราะบัญชี AI Studio ระดับฟรีไม่ได้รับโควตา Pro
เรียกแล้วได้ `429` ทันที (ถ้าอัปเกรดบัญชีเป็นแบบเสียเงินแล้วค่อยเพิ่มเข้าไป)

### สองข้อควรระวังของ Gemini ที่โค้ดนี้จัดการไว้แล้ว

1. **`maxOutputTokens` นับรวม token ที่ใช้คิดด้วย** ตั้งต่ำเกินไปจะได้ข้อความว่างกลับมา
   (คำถามเลขง่าย ๆ ใช้ token คิดไปเกือบพัน) จึงตั้งไว้ 8192 และถ้าเจอ
   `finishReason: MAX_TOKENS` พร้อมข้อความว่าง จะโยน error ที่อ่านรู้เรื่องแทนการส่งค่าว่าง
2. **ฝั่งผู้ช่วยเรียกว่า `model` ไม่ใช่ `assistant`** ต้องแปลง role ก่อนส่ง

### บริบทที่ส่งให้โมเดล

`buildDataContext()` สรุป payload เป็นข้อความประมาณ 8 พันตัวอักษร — ไม่ส่งข้อมูลดิบ
ทั้งก้อน (449KB) เพราะแพงและช้า ในบริบทมี KPI, ยอดรวมทุกรายงาน, สัดส่วนตามขนาด,
การจัดอันดับตามมิติต่าง ๆ, รอบปลูก, ผลเทียบยอดขนออก↔รับเข้า, finding ที่ร้ายแรง
และรายชื่อค่าที่มีจริง (สายพันธุ์/ลูกค้า/คลัง/ครอป)

### กฎของ Chatbot (อยู่ใน `SYSTEM_PROMPT`)

ตอบจากบริบทที่ให้เท่านั้น ห้ามเดาตัวเลข · อ้างหน่วยและชื่อรายงานกำกับเสมอ ·
เตือนเมื่อตัวเลขนั้นมีปัญหาคุณภาพ · `—` คือไม่มีข้อมูล ไม่ใช่ 0 ·
ถามเจาะจงคลัง/เดือน/ครอปไหน ต้องตอบของสิ่งนั้น ห้ามเอายอดรวมมาตอบแทน ·
"ขายดี" ดูจากรายงานการขาย ไม่ใช่รายงานสต็อก

เพิ่มสามข้อตอนแยกเป็นสองรายงาน:

- **"สต็อก" กำกวม** ต้องแยกว่าเป็นสต็อกดอก (กรัม) หรือสต็อกของใช้ (ชิ้น/กล่อง/ถัง)
- **รายได้/กำไรตอบได้แล้ว** จากชีตต้นทุน (เดิมห้ามตอบเพราะไม่มีข้อมูลเลย)
  แต่ยังห้ามแยกรายได้รายลูกค้า/สายพันธุ์ และห้ามคิด ASP เพราะชีตขายยังไม่มีราคา
- **เงินมาจากสามที่ คนละขอบเขต ห้ามบวกกัน** — งบบริษัท (ชีตต้นทุน) ·
  ต้นทุนวัสดุสิ้นเปลือง (Log Stock) · ยอดขายเป็นกรัม (ชีตขายดอก ไม่มีราคา)
- **ห้ามเอารายได้รวมไปหารกรัมที่ขายเพื่อเดา ASP** รายได้เป็นของทั้งบริษัท
  ไม่ได้ผูกกับรายการขายรายบรรทัด — และ **ห้ามหารต้นทุนด้วยผลผลิตเพื่อตอบต้นทุนต่อกรัม**

ข้อมูลวัสดุแนบไปกับบริบท **เฉพาะเมื่อมีอยู่ใน cache แล้ว** ไม่บังคับโหลดสด
เพราะจะทำให้คำถามแรกของวันช้าไปอีก 8 วินาทีทั้งที่ส่วนใหญ่ถามเรื่องดอก
และทุกจุดที่อ่าน `kpi.*` / `sources.*` ใน `chat-context.js` **ต้องกัน undefined**
ไม่งั้นรายงานที่โหลดแบบ lazy จะทำให้ `/api/chat` ตอบ 500

### การนับ usage และโควตา

- `usageStats` ใน `server.js` นับ token สะสมตั้งแต่เซิร์ฟเวอร์เริ่มทำงาน แยกรายโมเดล
  เก็บในหน่วยความจำอย่างเดียว รีสตาร์ทแล้วเริ่มนับใหม่ ดูที่ `GET /api/usage`
- **ไม่แปลง token เป็นเงิน** เพราะราคาขึ้นกับว่าบัญชี AI Studio เป็นระดับฟรีหรือแบบเสียเงิน
  ซึ่งเซิร์ฟเวอร์ไม่มีทางรู้เอง การเดาราคาแล้วแสดงเป็นตัวเลขจะทำให้เข้าใจผิด
- โควตารายวันแยกตามผู้ใช้ (`chatQuotaPerDay`) นับหลังเรียกสำเร็จเท่านั้น —
  API ล่มไม่ควรตัดสิทธิ์ผู้ใช้ รีเซ็ตตามวันแบบ UTC
